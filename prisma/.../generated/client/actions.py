# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class CountryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Country]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Country.prisma().query_raw(
            'SELECT * FROM Country WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Country
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Country.prisma().query_first(
            'SELECT * FROM Country WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.CountryCreateInput,
        include: Optional[types.CountryInclude] = None
    ) -> _PrismaModelT:
        """Create a new Country record.

        Parameters
        ----------
        data
            Country record data
        include
            Specifies which relations should be loaded on the returned Country model

        Returns
        -------
        prisma.models.Country
            The created Country record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Country record from just the required fields
        country = Country.prisma().create(
            data={
                # data to create a Country record
                'name': 'ggciceaie',
                'code': 'bbehjachib',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.CountryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Country records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Country record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Country.prisma().create_many(
            data=[
                {
                    # data to create a Country record
                    'name': 'cadfabfehe',
                    'code': 'dgiiaaijj',
                },
                {
                    # data to create a Country record
                    'name': 'bfaiacjjfc',
                    'code': 'eigcfgbif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.CountryWhereUniqueInput,
        include: Optional[types.CountryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Country record.

        Parameters
        ----------
        where
            Country filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Country model

        Returns
        -------
        prisma.models.Country
            The deleted Country record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        country = Country.prisma().delete(
            where={
                'id': 'bagcfbhiig',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.CountryWhereUniqueInput,
        include: Optional[types.CountryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Country record.

        Parameters
        ----------
        where
            Country filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Country model

        Returns
        -------
        prisma.models.Country
            The found Country record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        country = Country.prisma().find_unique(
            where={
                'id': 'cghideieh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.CountryWhereUniqueInput,
        include: Optional[types.CountryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Country record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Country filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Country model

        Returns
        -------
        prisma.models.Country
            The found Country record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        country = Country.prisma().find_unique_or_raise(
            where={
                'id': 'biabhbdai',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CountryWhereInput] = None,
        cursor: Optional[types.CountryWhereUniqueInput] = None,
        include: Optional[types.CountryInclude] = None,
        order: Optional[Union[types.CountryOrderByInput, List[types.CountryOrderByInput]]] = None,
        distinct: Optional[List[types.CountryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Country records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Country records returned
        skip
            Ignore the first N results
        where
            Country filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Country model
        order
            Order the returned Country records by any field
        distinct
            Filter Country records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Country]
            The list of all Country records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Country records
        countrys = Country.prisma().find_many(take=10)

        # find the first 5 Country records ordered by the code field
        countrys = Country.prisma().find_many(
            take=5,
            order={
                'code': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CountryWhereInput] = None,
        cursor: Optional[types.CountryWhereUniqueInput] = None,
        include: Optional[types.CountryInclude] = None,
        order: Optional[Union[types.CountryOrderByInput, List[types.CountryOrderByInput]]] = None,
        distinct: Optional[List[types.CountryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Country record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Country filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Country model
        order
            Order the returned Country records by any field
        distinct
            Filter Country records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Country
            The first Country record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Country record ordered by the createdAt field
        country = Country.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CountryWhereInput] = None,
        cursor: Optional[types.CountryWhereUniqueInput] = None,
        include: Optional[types.CountryInclude] = None,
        order: Optional[Union[types.CountryOrderByInput, List[types.CountryOrderByInput]]] = None,
        distinct: Optional[List[types.CountryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Country record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Country filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Country model
        order
            Order the returned Country records by any field
        distinct
            Filter Country records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Country
            The first Country record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Country record ordered by the updatedAt field
        country = Country.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.CountryUpdateInput,
        where: types.CountryWhereUniqueInput,
        include: Optional[types.CountryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Country record.

        Parameters
        ----------
        data
            Country record data specifying what to update
        where
            Country filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Country model

        Returns
        -------
        prisma.models.Country
            The updated Country record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        country = Country.prisma().update(
            where={
                'id': 'idghgaicb',
            },
            data={
                # data to update the Country record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.CountryWhereUniqueInput,
        data: types.CountryUpsertInput,
        include: Optional[types.CountryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Country filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Country model

        Returns
        -------
        prisma.models.Country
            The created or updated Country record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        country = Country.prisma().upsert(
            where={
                'id': 'fjfddhigg',
            },
            data={
                'create': {
                    'id': 'fjfddhigg',
                    'name': 'bfaiacjjfc',
                    'code': 'eigcfgbif',
                },
                'update': {
                    'name': 'bfaiacjjfc',
                    'code': 'eigcfgbif',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.CountryUpdateManyMutationInput,
        where: types.CountryWhereInput,
    ) -> int:
        """Update multiple Country records

        Parameters
        ----------
        data
            Country data to update the selected Country records to
        where
            Filter to select the Country records to update

        Returns
        -------
        int
            The total number of Country records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Country records
        total = Country.prisma().update_many(
            data={
                'id': 'hjaecfifb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CountryWhereInput] = None,
        cursor: Optional[types.CountryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Country records present in the database

        Parameters
        ----------
        select
            Select the Country fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Country filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CountryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Country.prisma().count()

        # results: prisma.types.CountryCountAggregateOutput
        results = Country.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.CountryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CountryWhereInput] = None,
        cursor: Optional[types.CountryWhereUniqueInput] = None,
    ) -> types.CountryCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.CountryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CountryWhereInput] = None,
        cursor: Optional[types.CountryWhereUniqueInput] = None,
    ) -> Union[int, types.CountryCountAggregateOutput]:
        """Count the number of Country records present in the database

        Parameters
        ----------
        select
            Select the Country fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Country filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CountryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Country.prisma().count()

        # results: prisma.types.CountryCountAggregateOutput
        results = Country.prisma().count(
            select={
                '_all': True,
                'code': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.CountryCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.CountryWhereInput] = None
    ) -> int:
        """Delete multiple Country records.

        Parameters
        ----------
        where
            Optional Country filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Country records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Country records
        total = Country.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.CountryScalarFieldKeys'],
        *,
        where: Optional['types.CountryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.CountryAvgAggregateInput'] = None,
        sum: Optional['types.CountrySumAggregateInput'] = None,
        min: Optional['types.CountryMinAggregateInput'] = None,
        max: Optional['types.CountryMaxAggregateInput'] = None,
        having: Optional['types.CountryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.CountryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.CountryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.CountryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.CountryGroupByOutput']:
        """Group Country records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Country fields to group records by
        where
            Country filter to select records
        take
            Limit the maximum number of Country records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.CountryGroupByOutput]
            A list of dictionaries representing the Country record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Country records by createdAt values
        # and count how many records are in each group
        results = Country.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StateActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.State]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = State.prisma().query_raw(
            'SELECT * FROM State WHERE id = $1',
            'cbbbjbfcii',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.State
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = State.prisma().query_first(
            'SELECT * FROM State WHERE name = $1',
            'bbejhfidcb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.StateCreateInput,
        include: Optional[types.StateInclude] = None
    ) -> _PrismaModelT:
        """Create a new State record.

        Parameters
        ----------
        data
            State record data
        include
            Specifies which relations should be loaded on the returned State model

        Returns
        -------
        prisma.models.State
            The created State record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a State record from just the required fields
        state = State.prisma().create(
            data={
                # data to create a State record
                'name': 'bgeecijdgg',
                'countryId': 'bdiicjafbj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.StateCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple State records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of State record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = State.prisma().create_many(
            data=[
                {
                    # data to create a State record
                    'name': 'bgehebiafc',
                    'countryId': 'bghffegacj',
                },
                {
                    # data to create a State record
                    'name': 'bhghchehcc',
                    'countryId': 'dcgchcbbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.StateWhereUniqueInput,
        include: Optional[types.StateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single State record.

        Parameters
        ----------
        where
            State filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned State model

        Returns
        -------
        prisma.models.State
            The deleted State record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        state = State.prisma().delete(
            where={
                'id': 'bdedcabahc',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.StateWhereUniqueInput,
        include: Optional[types.StateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique State record.

        Parameters
        ----------
        where
            State filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned State model

        Returns
        -------
        prisma.models.State
            The found State record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        state = State.prisma().find_unique(
            where={
                'id': 'ghfhiafcb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.StateWhereUniqueInput,
        include: Optional[types.StateInclude] = None
    ) -> _PrismaModelT:
        """Find a unique State record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            State filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned State model

        Returns
        -------
        prisma.models.State
            The found State record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        state = State.prisma().find_unique_or_raise(
            where={
                'id': 'heejgedji',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StateWhereInput] = None,
        cursor: Optional[types.StateWhereUniqueInput] = None,
        include: Optional[types.StateInclude] = None,
        order: Optional[Union[types.StateOrderByInput, List[types.StateOrderByInput]]] = None,
        distinct: Optional[List[types.StateScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple State records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of State records returned
        skip
            Ignore the first N results
        where
            State filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned State model
        order
            Order the returned State records by any field
        distinct
            Filter State records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.State]
            The list of all State records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 State records
        states = State.prisma().find_many(take=10)

        # find the first 5 State records ordered by the countryId field
        states = State.prisma().find_many(
            take=5,
            order={
                'countryId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StateWhereInput] = None,
        cursor: Optional[types.StateWhereUniqueInput] = None,
        include: Optional[types.StateInclude] = None,
        order: Optional[Union[types.StateOrderByInput, List[types.StateOrderByInput]]] = None,
        distinct: Optional[List[types.StateScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single State record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            State filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned State model
        order
            Order the returned State records by any field
        distinct
            Filter State records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.State
            The first State record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second State record ordered by the createdAt field
        state = State.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StateWhereInput] = None,
        cursor: Optional[types.StateWhereUniqueInput] = None,
        include: Optional[types.StateInclude] = None,
        order: Optional[Union[types.StateOrderByInput, List[types.StateOrderByInput]]] = None,
        distinct: Optional[List[types.StateScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single State record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            State filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned State model
        order
            Order the returned State records by any field
        distinct
            Filter State records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.State
            The first State record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second State record ordered by the updatedAt field
        state = State.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.StateUpdateInput,
        where: types.StateWhereUniqueInput,
        include: Optional[types.StateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single State record.

        Parameters
        ----------
        data
            State record data specifying what to update
        where
            State filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned State model

        Returns
        -------
        prisma.models.State
            The updated State record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        state = State.prisma().update(
            where={
                'id': 'bjgjgibgbf',
            },
            data={
                # data to update the State record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.StateWhereUniqueInput,
        data: types.StateUpsertInput,
        include: Optional[types.StateInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            State filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned State model

        Returns
        -------
        prisma.models.State
            The created or updated State record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        state = State.prisma().upsert(
            where={
                'id': 'bbbgbhfjge',
            },
            data={
                'create': {
                    'id': 'bbbgbhfjge',
                    'name': 'bhghchehcc',
                    'countryId': 'dcgchcbbf',
                },
                'update': {
                    'name': 'bhghchehcc',
                    'countryId': 'dcgchcbbf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.StateUpdateManyMutationInput,
        where: types.StateWhereInput,
    ) -> int:
        """Update multiple State records

        Parameters
        ----------
        data
            State data to update the selected State records to
        where
            Filter to select the State records to update

        Returns
        -------
        int
            The total number of State records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all State records
        total = State.prisma().update_many(
            data={
                'id': 'igbehcbab'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StateWhereInput] = None,
        cursor: Optional[types.StateWhereUniqueInput] = None,
    ) -> int:
        """Count the number of State records present in the database

        Parameters
        ----------
        select
            Select the State fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            State filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = State.prisma().count()

        # results: prisma.types.StateCountAggregateOutput
        results = State.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.StateCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StateWhereInput] = None,
        cursor: Optional[types.StateWhereUniqueInput] = None,
    ) -> types.StateCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.StateCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StateWhereInput] = None,
        cursor: Optional[types.StateWhereUniqueInput] = None,
    ) -> Union[int, types.StateCountAggregateOutput]:
        """Count the number of State records present in the database

        Parameters
        ----------
        select
            Select the State fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            State filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = State.prisma().count()

        # results: prisma.types.StateCountAggregateOutput
        results = State.prisma().count(
            select={
                '_all': True,
                'countryId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StateCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.StateWhereInput] = None
    ) -> int:
        """Delete multiple State records.

        Parameters
        ----------
        where
            Optional State filter to find the records to be deleted

        Returns
        -------
        int
            The total number of State records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all State records
        total = State.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.StateScalarFieldKeys'],
        *,
        where: Optional['types.StateWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StateAvgAggregateInput'] = None,
        sum: Optional['types.StateSumAggregateInput'] = None,
        min: Optional['types.StateMinAggregateInput'] = None,
        max: Optional['types.StateMaxAggregateInput'] = None,
        having: Optional['types.StateScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StateCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StateScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StateScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StateGroupByOutput']:
        """Group State records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar State fields to group records by
        where
            State filter to select records
        take
            Limit the maximum number of State records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StateGroupByOutput]
            A list of dictionaries representing the State record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group State records by createdAt values
        # and count how many records are in each group
        results = State.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class CityActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.City]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = City.prisma().query_raw(
            'SELECT * FROM City WHERE id = $1',
            'bdadaadhag',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.City
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = City.prisma().query_first(
            'SELECT * FROM City WHERE name = $1',
            'bgiggdidbf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.CityCreateInput,
        include: Optional[types.CityInclude] = None
    ) -> _PrismaModelT:
        """Create a new City record.

        Parameters
        ----------
        data
            City record data
        include
            Specifies which relations should be loaded on the returned City model

        Returns
        -------
        prisma.models.City
            The created City record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a City record from just the required fields
        city = City.prisma().create(
            data={
                # data to create a City record
                'name': 'caaaedabfc',
                'stateId': 'bigibebcib',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.CityCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple City records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of City record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = City.prisma().create_many(
            data=[
                {
                    # data to create a City record
                    'name': 'bigaiehgcc',
                    'stateId': 'beeifcbebf',
                },
                {
                    # data to create a City record
                    'name': 'bgcigfahea',
                    'stateId': 'bcejgaggif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.CityWhereUniqueInput,
        include: Optional[types.CityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single City record.

        Parameters
        ----------
        where
            City filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned City model

        Returns
        -------
        prisma.models.City
            The deleted City record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        city = City.prisma().delete(
            where={
                'id': 'idfjadbcc',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.CityWhereUniqueInput,
        include: Optional[types.CityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique City record.

        Parameters
        ----------
        where
            City filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned City model

        Returns
        -------
        prisma.models.City
            The found City record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        city = City.prisma().find_unique(
            where={
                'id': 'hgdhbjhhj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.CityWhereUniqueInput,
        include: Optional[types.CityInclude] = None
    ) -> _PrismaModelT:
        """Find a unique City record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            City filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned City model

        Returns
        -------
        prisma.models.City
            The found City record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        city = City.prisma().find_unique_or_raise(
            where={
                'id': 'ecjjjfbae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CityWhereInput] = None,
        cursor: Optional[types.CityWhereUniqueInput] = None,
        include: Optional[types.CityInclude] = None,
        order: Optional[Union[types.CityOrderByInput, List[types.CityOrderByInput]]] = None,
        distinct: Optional[List[types.CityScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple City records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of City records returned
        skip
            Ignore the first N results
        where
            City filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned City model
        order
            Order the returned City records by any field
        distinct
            Filter City records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.City]
            The list of all City records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 City records
        citys = City.prisma().find_many(take=10)

        # find the first 5 City records ordered by the stateId field
        citys = City.prisma().find_many(
            take=5,
            order={
                'stateId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CityWhereInput] = None,
        cursor: Optional[types.CityWhereUniqueInput] = None,
        include: Optional[types.CityInclude] = None,
        order: Optional[Union[types.CityOrderByInput, List[types.CityOrderByInput]]] = None,
        distinct: Optional[List[types.CityScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single City record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            City filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned City model
        order
            Order the returned City records by any field
        distinct
            Filter City records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.City
            The first City record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second City record ordered by the createdAt field
        city = City.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CityWhereInput] = None,
        cursor: Optional[types.CityWhereUniqueInput] = None,
        include: Optional[types.CityInclude] = None,
        order: Optional[Union[types.CityOrderByInput, List[types.CityOrderByInput]]] = None,
        distinct: Optional[List[types.CityScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single City record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            City filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned City model
        order
            Order the returned City records by any field
        distinct
            Filter City records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.City
            The first City record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second City record ordered by the updatedAt field
        city = City.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.CityUpdateInput,
        where: types.CityWhereUniqueInput,
        include: Optional[types.CityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single City record.

        Parameters
        ----------
        data
            City record data specifying what to update
        where
            City filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned City model

        Returns
        -------
        prisma.models.City
            The updated City record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        city = City.prisma().update(
            where={
                'id': 'bhhfibbigf',
            },
            data={
                # data to update the City record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.CityWhereUniqueInput,
        data: types.CityUpsertInput,
        include: Optional[types.CityInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            City filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned City model

        Returns
        -------
        prisma.models.City
            The created or updated City record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        city = City.prisma().upsert(
            where={
                'id': 'ijdbeffgg',
            },
            data={
                'create': {
                    'id': 'ijdbeffgg',
                    'name': 'bgcigfahea',
                    'stateId': 'bcejgaggif',
                },
                'update': {
                    'name': 'bgcigfahea',
                    'stateId': 'bcejgaggif',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.CityUpdateManyMutationInput,
        where: types.CityWhereInput,
    ) -> int:
        """Update multiple City records

        Parameters
        ----------
        data
            City data to update the selected City records to
        where
            Filter to select the City records to update

        Returns
        -------
        int
            The total number of City records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all City records
        total = City.prisma().update_many(
            data={
                'id': 'jjfeafhfj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CityWhereInput] = None,
        cursor: Optional[types.CityWhereUniqueInput] = None,
    ) -> int:
        """Count the number of City records present in the database

        Parameters
        ----------
        select
            Select the City fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            City filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = City.prisma().count()

        # results: prisma.types.CityCountAggregateOutput
        results = City.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.CityCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CityWhereInput] = None,
        cursor: Optional[types.CityWhereUniqueInput] = None,
    ) -> types.CityCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.CityCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CityWhereInput] = None,
        cursor: Optional[types.CityWhereUniqueInput] = None,
    ) -> Union[int, types.CityCountAggregateOutput]:
        """Count the number of City records present in the database

        Parameters
        ----------
        select
            Select the City fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            City filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = City.prisma().count()

        # results: prisma.types.CityCountAggregateOutput
        results = City.prisma().count(
            select={
                '_all': True,
                'stateId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.CityCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.CityWhereInput] = None
    ) -> int:
        """Delete multiple City records.

        Parameters
        ----------
        where
            Optional City filter to find the records to be deleted

        Returns
        -------
        int
            The total number of City records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all City records
        total = City.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.CityScalarFieldKeys'],
        *,
        where: Optional['types.CityWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.CityAvgAggregateInput'] = None,
        sum: Optional['types.CitySumAggregateInput'] = None,
        min: Optional['types.CityMinAggregateInput'] = None,
        max: Optional['types.CityMaxAggregateInput'] = None,
        having: Optional['types.CityScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.CityCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.CityScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.CityScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.CityGroupByOutput']:
        """Group City records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar City fields to group records by
        where
            City filter to select records
        take
            Limit the maximum number of City records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.CityGroupByOutput]
            A list of dictionaries representing the City record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group City records by createdAt values
        # and count how many records are in each group
        results = City.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.User]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = User.prisma().query_raw(
            'SELECT * FROM User WHERE id = $1',
            'cbachdgfce',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.User
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().query_first(
            'SELECT * FROM User WHERE email = $1',
            'chbfcacbd',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UserCreateInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Create a new User record.

        Parameters
        ----------
        data
            User record data
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created User record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a User record from just the required fields
        user = User.prisma().create(
            data={
                # data to create a User record
                'email': 'efggddide',
                'firstname': 'caficfigfb',
                'lastname': 'bfidgijfjc',
                'birthdate': datetime.datetime.utcnow(),
                'countryId': 'ihieecagf',
                'stateId': 'bghfciaafe',
                'church': 'bgchfhgceh',
                'phone': 'cafeiaccbc',
                'password': 'gaddfhfh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple User records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of User record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = User.prisma().create_many(
            data=[
                {
                    # data to create a User record
                    'email': 'gieegcbeg',
                    'firstname': 'bgcffadich',
                    'lastname': 'fcbichhci',
                    'birthdate': datetime.datetime.utcnow(),
                    'countryId': 'bcggadccgf',
                    'stateId': 'jdcfdcgc',
                    'church': 'cafdaehjid',
                    'phone': 'gifdddbia',
                    'password': 'bchehecef',
                },
                {
                    # data to create a User record
                    'email': 'jeijcbhfe',
                    'firstname': 'bjgejjabff',
                    'lastname': 'bcciijbibg',
                    'birthdate': datetime.datetime.utcnow(),
                    'countryId': 'cffcachfd',
                    'stateId': 'bccdfhdigc',
                    'church': 'febcgjbfj',
                    'phone': 'bageiegghg',
                    'password': 'faidicegb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single User record.

        Parameters
        ----------
        where
            User filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The deleted User record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().delete(
            where={
                'id': 'bacecgfhbe',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique User record.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique(
            where={
                'id': 'ihcahiead',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique User record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique_or_raise(
            where={
                'id': 'biheheiajg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple User records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N results
        where
            User filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.User]
            The list of all User records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 User records
        users = User.prisma().find_many(take=10)

        # find the first 5 User records ordered by the firstname field
        users = User.prisma().find_many(
            take=5,
            order={
                'firstname': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single User record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the lastname field
        user = User.prisma().find_first(
            skip=1,
            order={
                'lastname': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single User record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the birthdate field
        user = User.prisma().find_first_or_raise(
            skip=1,
            order={
                'birthdate': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UserUpdateInput,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single User record.

        Parameters
        ----------
        data
            User record data specifying what to update
        where
            User filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The updated User record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().update(
            where={
                'id': 'jbgijghgb',
            },
            data={
                # data to update the User record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UserWhereUniqueInput,
        data: types.UserUpsertInput,
        include: Optional[types.UserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            User filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created or updated User record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().upsert(
            where={
                'id': 'hgjcghfbi',
            },
            data={
                'create': {
                    'id': 'hgjcghfbi',
                    'email': 'jeijcbhfe',
                    'firstname': 'bjgejjabff',
                    'lastname': 'bcciijbibg',
                    'birthdate': datetime.datetime.utcnow(),
                    'countryId': 'cffcachfd',
                    'stateId': 'bccdfhdigc',
                    'church': 'febcgjbfj',
                    'phone': 'bageiegghg',
                    'password': 'faidicegb',
                },
                'update': {
                    'email': 'jeijcbhfe',
                    'firstname': 'bjgejjabff',
                    'lastname': 'bcciijbibg',
                    'birthdate': datetime.datetime.utcnow(),
                    'countryId': 'cffcachfd',
                    'stateId': 'bccdfhdigc',
                    'church': 'febcgjbfj',
                    'phone': 'bageiegghg',
                    'password': 'faidicegb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UserUpdateManyMutationInput,
        where: types.UserWhereInput,
    ) -> int:
        """Update multiple User records

        Parameters
        ----------
        data
            User data to update the selected User records to
        where
            Filter to select the User records to update

        Returns
        -------
        int
            The total number of User records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all User records
        total = User.prisma().update_many(
            data={
                'countryId': 'icadbcehj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'stateId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> types.UserCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> Union[int, types.UserCountAggregateOutput]:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'church': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UserWhereInput] = None
    ) -> int:
        """Delete multiple User records.

        Parameters
        ----------
        where
            Optional User filter to find the records to be deleted

        Returns
        -------
        int
            The total number of User records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all User records
        total = User.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UserScalarFieldKeys'],
        *,
        where: Optional['types.UserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserAvgAggregateInput'] = None,
        sum: Optional['types.UserSumAggregateInput'] = None,
        min: Optional['types.UserMinAggregateInput'] = None,
        max: Optional['types.UserMaxAggregateInput'] = None,
        having: Optional['types.UserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserGroupByOutput']:
        """Group User records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar User fields to group records by
        where
            User filter to select records
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserGroupByOutput]
            A list of dictionaries representing the User record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group User records by phone values
        # and count how many records are in each group
        results = User.prisma().group_by(
            ['phone'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SessionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Session]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Session.prisma().query_raw(
            'SELECT * FROM Session WHERE id = $1',
            'jchciaee',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Session
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Session.prisma().query_first(
            'SELECT * FROM Session WHERE userId = $1',
            'deeificjd',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.SessionCreateInput,
        include: Optional[types.SessionInclude] = None
    ) -> _PrismaModelT:
        """Create a new Session record.

        Parameters
        ----------
        data
            Session record data
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The created Session record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Session record from just the required fields
        session = Session.prisma().create(
            data={
                # data to create a Session record
                'userId': 'bbcbhebbda',
                'token': 'bejfijgcfb',
                'expiresAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.SessionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Session records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Session record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Session.prisma().create_many(
            data=[
                {
                    # data to create a Session record
                    'userId': 'caifcbgii',
                    'token': 'igaibbfgj',
                    'expiresAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a Session record
                    'userId': 'bggajdcbbi',
                    'token': 'fcfhgbjed',
                    'expiresAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Session record.

        Parameters
        ----------
        where
            Session filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The deleted Session record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().delete(
            where={
                'id': 'hdgcajhjg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Session record.

        Parameters
        ----------
        where
            Session filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The found Session record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().find_unique(
            where={
                'id': 'ejdjahicb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Session record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Session filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The found Session record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().find_unique_or_raise(
            where={
                'id': 'gdjgigfgc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Session records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Session records returned
        skip
            Ignore the first N results
        where
            Session filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Session]
            The list of all Session records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Session records
        sessions = Session.prisma().find_many(take=10)

        # find the first 5 Session records ordered by the token field
        sessions = Session.prisma().find_many(
            take=5,
            order={
                'token': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Session record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Session filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Session
            The first Session record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Session record ordered by the createdAt field
        session = Session.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Session record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Session filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Session
            The first Session record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Session record ordered by the expiresAt field
        session = Session.prisma().find_first_or_raise(
            skip=1,
            order={
                'expiresAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.SessionUpdateInput,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Session record.

        Parameters
        ----------
        data
            Session record data specifying what to update
        where
            Session filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The updated Session record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        session = Session.prisma().update(
            where={
                'id': 'gfeaahdeh',
            },
            data={
                # data to update the Session record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.SessionWhereUniqueInput,
        data: types.SessionUpsertInput,
        include: Optional[types.SessionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Session filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The created or updated Session record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().upsert(
            where={
                'id': 'bjafcgbffc',
            },
            data={
                'create': {
                    'id': 'bjafcgbffc',
                    'userId': 'bggajdcbbi',
                    'token': 'fcfhgbjed',
                    'expiresAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'userId': 'bggajdcbbi',
                    'token': 'fcfhgbjed',
                    'expiresAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.SessionUpdateManyMutationInput,
        where: types.SessionWhereInput,
    ) -> int:
        """Update multiple Session records

        Parameters
        ----------
        data
            Session data to update the selected Session records to
        where
            Filter to select the Session records to update

        Returns
        -------
        int
            The total number of Session records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Session records
        total = Session.prisma().update_many(
            data={
                'id': 'hihegjif'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Session records present in the database

        Parameters
        ----------
        select
            Select the Session fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Session filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Session.prisma().count()

        # results: prisma.types.SessionCountAggregateOutput
        results = Session.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.SessionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> types.SessionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.SessionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> Union[int, types.SessionCountAggregateOutput]:
        """Count the number of Session records present in the database

        Parameters
        ----------
        select
            Select the Session fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Session filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Session.prisma().count()

        # results: prisma.types.SessionCountAggregateOutput
        results = Session.prisma().count(
            select={
                '_all': True,
                'token': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SessionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.SessionWhereInput] = None
    ) -> int:
        """Delete multiple Session records.

        Parameters
        ----------
        where
            Optional Session filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Session records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Session records
        total = Session.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.SessionScalarFieldKeys'],
        *,
        where: Optional['types.SessionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SessionAvgAggregateInput'] = None,
        sum: Optional['types.SessionSumAggregateInput'] = None,
        min: Optional['types.SessionMinAggregateInput'] = None,
        max: Optional['types.SessionMaxAggregateInput'] = None,
        having: Optional['types.SessionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SessionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SessionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SessionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SessionGroupByOutput']:
        """Group Session records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Session fields to group records by
        where
            Session filter to select records
        take
            Limit the maximum number of Session records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SessionGroupByOutput]
            A list of dictionaries representing the Session record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Session records by createdAt values
        # and count how many records are in each group
        results = Session.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PasswordResetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PasswordReset]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PasswordReset.prisma().query_raw(
            'SELECT * FROM PasswordReset WHERE id = $1',
            'bdjidcidac',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PasswordReset
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PasswordReset.prisma().query_first(
            'SELECT * FROM PasswordReset WHERE userId = $1',
            'ifgaaagff',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PasswordResetCreateInput,
        include: Optional[types.PasswordResetInclude] = None
    ) -> _PrismaModelT:
        """Create a new PasswordReset record.

        Parameters
        ----------
        data
            PasswordReset record data
        include
            Specifies which relations should be loaded on the returned PasswordReset model

        Returns
        -------
        prisma.models.PasswordReset
            The created PasswordReset record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PasswordReset record from just the required fields
        passwordreset = PasswordReset.prisma().create(
            data={
                # data to create a PasswordReset record
                'userId': 'befcddgjce',
                'token': 'bfhdbjjgfd',
                'expiresAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PasswordResetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PasswordReset records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PasswordReset record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PasswordReset.prisma().create_many(
            data=[
                {
                    # data to create a PasswordReset record
                    'userId': 'cabdjadaji',
                    'token': 'faajgfadf',
                    'expiresAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a PasswordReset record
                    'userId': 'biaagcedjc',
                    'token': 'cahhaghecf',
                    'expiresAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PasswordResetWhereUniqueInput,
        include: Optional[types.PasswordResetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PasswordReset record.

        Parameters
        ----------
        where
            PasswordReset filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PasswordReset model

        Returns
        -------
        prisma.models.PasswordReset
            The deleted PasswordReset record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        passwordreset = PasswordReset.prisma().delete(
            where={
                'id': 'bghcbbcidi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PasswordResetWhereUniqueInput,
        include: Optional[types.PasswordResetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PasswordReset record.

        Parameters
        ----------
        where
            PasswordReset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PasswordReset model

        Returns
        -------
        prisma.models.PasswordReset
            The found PasswordReset record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        passwordreset = PasswordReset.prisma().find_unique(
            where={
                'id': 'jcgghhgdj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PasswordResetWhereUniqueInput,
        include: Optional[types.PasswordResetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PasswordReset record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PasswordReset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PasswordReset model

        Returns
        -------
        prisma.models.PasswordReset
            The found PasswordReset record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        passwordreset = PasswordReset.prisma().find_unique_or_raise(
            where={
                'id': 'beehgcebbg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PasswordResetWhereInput] = None,
        cursor: Optional[types.PasswordResetWhereUniqueInput] = None,
        include: Optional[types.PasswordResetInclude] = None,
        order: Optional[Union[types.PasswordResetOrderByInput, List[types.PasswordResetOrderByInput]]] = None,
        distinct: Optional[List[types.PasswordResetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PasswordReset records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PasswordReset records returned
        skip
            Ignore the first N results
        where
            PasswordReset filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PasswordReset model
        order
            Order the returned PasswordReset records by any field
        distinct
            Filter PasswordReset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PasswordReset]
            The list of all PasswordReset records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PasswordReset records
        passwordresets = PasswordReset.prisma().find_many(take=10)

        # find the first 5 PasswordReset records ordered by the token field
        passwordresets = PasswordReset.prisma().find_many(
            take=5,
            order={
                'token': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PasswordResetWhereInput] = None,
        cursor: Optional[types.PasswordResetWhereUniqueInput] = None,
        include: Optional[types.PasswordResetInclude] = None,
        order: Optional[Union[types.PasswordResetOrderByInput, List[types.PasswordResetOrderByInput]]] = None,
        distinct: Optional[List[types.PasswordResetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PasswordReset record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PasswordReset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PasswordReset model
        order
            Order the returned PasswordReset records by any field
        distinct
            Filter PasswordReset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PasswordReset
            The first PasswordReset record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PasswordReset record ordered by the createdAt field
        passwordreset = PasswordReset.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PasswordResetWhereInput] = None,
        cursor: Optional[types.PasswordResetWhereUniqueInput] = None,
        include: Optional[types.PasswordResetInclude] = None,
        order: Optional[Union[types.PasswordResetOrderByInput, List[types.PasswordResetOrderByInput]]] = None,
        distinct: Optional[List[types.PasswordResetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PasswordReset record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PasswordReset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PasswordReset model
        order
            Order the returned PasswordReset records by any field
        distinct
            Filter PasswordReset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PasswordReset
            The first PasswordReset record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PasswordReset record ordered by the expiresAt field
        passwordreset = PasswordReset.prisma().find_first_or_raise(
            skip=1,
            order={
                'expiresAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PasswordResetUpdateInput,
        where: types.PasswordResetWhereUniqueInput,
        include: Optional[types.PasswordResetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PasswordReset record.

        Parameters
        ----------
        data
            PasswordReset record data specifying what to update
        where
            PasswordReset filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PasswordReset model

        Returns
        -------
        prisma.models.PasswordReset
            The updated PasswordReset record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        passwordreset = PasswordReset.prisma().update(
            where={
                'id': 'bhdiaidiaf',
            },
            data={
                # data to update the PasswordReset record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PasswordResetWhereUniqueInput,
        data: types.PasswordResetUpsertInput,
        include: Optional[types.PasswordResetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PasswordReset filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PasswordReset model

        Returns
        -------
        prisma.models.PasswordReset
            The created or updated PasswordReset record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        passwordreset = PasswordReset.prisma().upsert(
            where={
                'id': 'deajegcfi',
            },
            data={
                'create': {
                    'id': 'deajegcfi',
                    'userId': 'biaagcedjc',
                    'token': 'cahhaghecf',
                    'expiresAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'userId': 'biaagcedjc',
                    'token': 'cahhaghecf',
                    'expiresAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PasswordResetUpdateManyMutationInput,
        where: types.PasswordResetWhereInput,
    ) -> int:
        """Update multiple PasswordReset records

        Parameters
        ----------
        data
            PasswordReset data to update the selected PasswordReset records to
        where
            Filter to select the PasswordReset records to update

        Returns
        -------
        int
            The total number of PasswordReset records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PasswordReset records
        total = PasswordReset.prisma().update_many(
            data={
                'id': 'gabahhhjf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PasswordResetWhereInput] = None,
        cursor: Optional[types.PasswordResetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PasswordReset records present in the database

        Parameters
        ----------
        select
            Select the PasswordReset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PasswordReset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PasswordResetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PasswordReset.prisma().count()

        # results: prisma.types.PasswordResetCountAggregateOutput
        results = PasswordReset.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PasswordResetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PasswordResetWhereInput] = None,
        cursor: Optional[types.PasswordResetWhereUniqueInput] = None,
    ) -> types.PasswordResetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PasswordResetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PasswordResetWhereInput] = None,
        cursor: Optional[types.PasswordResetWhereUniqueInput] = None,
    ) -> Union[int, types.PasswordResetCountAggregateOutput]:
        """Count the number of PasswordReset records present in the database

        Parameters
        ----------
        select
            Select the PasswordReset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PasswordReset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PasswordResetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PasswordReset.prisma().count()

        # results: prisma.types.PasswordResetCountAggregateOutput
        results = PasswordReset.prisma().count(
            select={
                '_all': True,
                'token': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PasswordResetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PasswordResetWhereInput] = None
    ) -> int:
        """Delete multiple PasswordReset records.

        Parameters
        ----------
        where
            Optional PasswordReset filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PasswordReset records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PasswordReset records
        total = PasswordReset.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PasswordResetScalarFieldKeys'],
        *,
        where: Optional['types.PasswordResetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PasswordResetAvgAggregateInput'] = None,
        sum: Optional['types.PasswordResetSumAggregateInput'] = None,
        min: Optional['types.PasswordResetMinAggregateInput'] = None,
        max: Optional['types.PasswordResetMaxAggregateInput'] = None,
        having: Optional['types.PasswordResetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PasswordResetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PasswordResetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PasswordResetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PasswordResetGroupByOutput']:
        """Group PasswordReset records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PasswordReset fields to group records by
        where
            PasswordReset filter to select records
        take
            Limit the maximum number of PasswordReset records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PasswordResetGroupByOutput]
            A list of dictionaries representing the PasswordReset record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PasswordReset records by createdAt values
        # and count how many records are in each group
        results = PasswordReset.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class EmailVerificationActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.EmailVerification]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = EmailVerification.prisma().query_raw(
            'SELECT * FROM EmailVerification WHERE id = $1',
            'cjagadcjg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.EmailVerification
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = EmailVerification.prisma().query_first(
            'SELECT * FROM EmailVerification WHERE userId = $1',
            'bifficggej',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.EmailVerificationCreateInput,
        include: Optional[types.EmailVerificationInclude] = None
    ) -> _PrismaModelT:
        """Create a new EmailVerification record.

        Parameters
        ----------
        data
            EmailVerification record data
        include
            Specifies which relations should be loaded on the returned EmailVerification model

        Returns
        -------
        prisma.models.EmailVerification
            The created EmailVerification record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a EmailVerification record from just the required fields
        emailverification = EmailVerification.prisma().create(
            data={
                # data to create a EmailVerification record
                'userId': 'bgbbaajbic',
                'token': 'eegghdhjb',
                'expiresAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.EmailVerificationCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple EmailVerification records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of EmailVerification record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = EmailVerification.prisma().create_many(
            data=[
                {
                    # data to create a EmailVerification record
                    'userId': 'daafgidjg',
                    'token': 'gdcgcgagj',
                    'expiresAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a EmailVerification record
                    'userId': 'bhceabbgja',
                    'token': 'ehabfhegh',
                    'expiresAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.EmailVerificationWhereUniqueInput,
        include: Optional[types.EmailVerificationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single EmailVerification record.

        Parameters
        ----------
        where
            EmailVerification filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned EmailVerification model

        Returns
        -------
        prisma.models.EmailVerification
            The deleted EmailVerification record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        emailverification = EmailVerification.prisma().delete(
            where={
                'id': 'bcajcajjbc',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.EmailVerificationWhereUniqueInput,
        include: Optional[types.EmailVerificationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique EmailVerification record.

        Parameters
        ----------
        where
            EmailVerification filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned EmailVerification model

        Returns
        -------
        prisma.models.EmailVerification
            The found EmailVerification record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        emailverification = EmailVerification.prisma().find_unique(
            where={
                'id': 'bfdgheeegf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.EmailVerificationWhereUniqueInput,
        include: Optional[types.EmailVerificationInclude] = None
    ) -> _PrismaModelT:
        """Find a unique EmailVerification record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            EmailVerification filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned EmailVerification model

        Returns
        -------
        prisma.models.EmailVerification
            The found EmailVerification record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        emailverification = EmailVerification.prisma().find_unique_or_raise(
            where={
                'id': 'ececbijji',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmailVerificationWhereInput] = None,
        cursor: Optional[types.EmailVerificationWhereUniqueInput] = None,
        include: Optional[types.EmailVerificationInclude] = None,
        order: Optional[Union[types.EmailVerificationOrderByInput, List[types.EmailVerificationOrderByInput]]] = None,
        distinct: Optional[List[types.EmailVerificationScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple EmailVerification records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of EmailVerification records returned
        skip
            Ignore the first N results
        where
            EmailVerification filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned EmailVerification model
        order
            Order the returned EmailVerification records by any field
        distinct
            Filter EmailVerification records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.EmailVerification]
            The list of all EmailVerification records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 EmailVerification records
        emailverifications = EmailVerification.prisma().find_many(take=10)

        # find the first 5 EmailVerification records ordered by the token field
        emailverifications = EmailVerification.prisma().find_many(
            take=5,
            order={
                'token': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EmailVerificationWhereInput] = None,
        cursor: Optional[types.EmailVerificationWhereUniqueInput] = None,
        include: Optional[types.EmailVerificationInclude] = None,
        order: Optional[Union[types.EmailVerificationOrderByInput, List[types.EmailVerificationOrderByInput]]] = None,
        distinct: Optional[List[types.EmailVerificationScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single EmailVerification record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            EmailVerification filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned EmailVerification model
        order
            Order the returned EmailVerification records by any field
        distinct
            Filter EmailVerification records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.EmailVerification
            The first EmailVerification record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second EmailVerification record ordered by the createdAt field
        emailverification = EmailVerification.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EmailVerificationWhereInput] = None,
        cursor: Optional[types.EmailVerificationWhereUniqueInput] = None,
        include: Optional[types.EmailVerificationInclude] = None,
        order: Optional[Union[types.EmailVerificationOrderByInput, List[types.EmailVerificationOrderByInput]]] = None,
        distinct: Optional[List[types.EmailVerificationScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single EmailVerification record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            EmailVerification filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned EmailVerification model
        order
            Order the returned EmailVerification records by any field
        distinct
            Filter EmailVerification records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.EmailVerification
            The first EmailVerification record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second EmailVerification record ordered by the expiresAt field
        emailverification = EmailVerification.prisma().find_first_or_raise(
            skip=1,
            order={
                'expiresAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.EmailVerificationUpdateInput,
        where: types.EmailVerificationWhereUniqueInput,
        include: Optional[types.EmailVerificationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single EmailVerification record.

        Parameters
        ----------
        data
            EmailVerification record data specifying what to update
        where
            EmailVerification filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned EmailVerification model

        Returns
        -------
        prisma.models.EmailVerification
            The updated EmailVerification record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        emailverification = EmailVerification.prisma().update(
            where={
                'id': 'cbcfgdcdhf',
            },
            data={
                # data to update the EmailVerification record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.EmailVerificationWhereUniqueInput,
        data: types.EmailVerificationUpsertInput,
        include: Optional[types.EmailVerificationInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            EmailVerification filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned EmailVerification model

        Returns
        -------
        prisma.models.EmailVerification
            The created or updated EmailVerification record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        emailverification = EmailVerification.prisma().upsert(
            where={
                'id': 'fdgjfbhia',
            },
            data={
                'create': {
                    'id': 'fdgjfbhia',
                    'userId': 'bhceabbgja',
                    'token': 'ehabfhegh',
                    'expiresAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'userId': 'bhceabbgja',
                    'token': 'ehabfhegh',
                    'expiresAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.EmailVerificationUpdateManyMutationInput,
        where: types.EmailVerificationWhereInput,
    ) -> int:
        """Update multiple EmailVerification records

        Parameters
        ----------
        data
            EmailVerification data to update the selected EmailVerification records to
        where
            Filter to select the EmailVerification records to update

        Returns
        -------
        int
            The total number of EmailVerification records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all EmailVerification records
        total = EmailVerification.prisma().update_many(
            data={
                'id': 'jcehcdchh'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmailVerificationWhereInput] = None,
        cursor: Optional[types.EmailVerificationWhereUniqueInput] = None,
    ) -> int:
        """Count the number of EmailVerification records present in the database

        Parameters
        ----------
        select
            Select the EmailVerification fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            EmailVerification filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EmailVerificationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = EmailVerification.prisma().count()

        # results: prisma.types.EmailVerificationCountAggregateOutput
        results = EmailVerification.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.EmailVerificationCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmailVerificationWhereInput] = None,
        cursor: Optional[types.EmailVerificationWhereUniqueInput] = None,
    ) -> types.EmailVerificationCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.EmailVerificationCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmailVerificationWhereInput] = None,
        cursor: Optional[types.EmailVerificationWhereUniqueInput] = None,
    ) -> Union[int, types.EmailVerificationCountAggregateOutput]:
        """Count the number of EmailVerification records present in the database

        Parameters
        ----------
        select
            Select the EmailVerification fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            EmailVerification filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EmailVerificationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = EmailVerification.prisma().count()

        # results: prisma.types.EmailVerificationCountAggregateOutput
        results = EmailVerification.prisma().count(
            select={
                '_all': True,
                'token': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.EmailVerificationCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.EmailVerificationWhereInput] = None
    ) -> int:
        """Delete multiple EmailVerification records.

        Parameters
        ----------
        where
            Optional EmailVerification filter to find the records to be deleted

        Returns
        -------
        int
            The total number of EmailVerification records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all EmailVerification records
        total = EmailVerification.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.EmailVerificationScalarFieldKeys'],
        *,
        where: Optional['types.EmailVerificationWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.EmailVerificationAvgAggregateInput'] = None,
        sum: Optional['types.EmailVerificationSumAggregateInput'] = None,
        min: Optional['types.EmailVerificationMinAggregateInput'] = None,
        max: Optional['types.EmailVerificationMaxAggregateInput'] = None,
        having: Optional['types.EmailVerificationScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.EmailVerificationCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.EmailVerificationScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.EmailVerificationScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.EmailVerificationGroupByOutput']:
        """Group EmailVerification records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar EmailVerification fields to group records by
        where
            EmailVerification filter to select records
        take
            Limit the maximum number of EmailVerification records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.EmailVerificationGroupByOutput]
            A list of dictionaries representing the EmailVerification record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group EmailVerification records by createdAt values
        # and count how many records are in each group
        results = EmailVerification.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class OAuthProviderActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.OAuthProvider]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = OAuthProvider.prisma().query_raw(
            'SELECT * FROM OAuthProvider WHERE id = $1',
            'bgcbjdhjcc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.OAuthProvider
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = OAuthProvider.prisma().query_first(
            'SELECT * FROM OAuthProvider WHERE userId = $1',
            'bieiidcabj',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.OAuthProviderCreateInput,
        include: Optional[types.OAuthProviderInclude] = None
    ) -> _PrismaModelT:
        """Create a new OAuthProvider record.

        Parameters
        ----------
        data
            OAuthProvider record data
        include
            Specifies which relations should be loaded on the returned OAuthProvider model

        Returns
        -------
        prisma.models.OAuthProvider
            The created OAuthProvider record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a OAuthProvider record from just the required fields
        oauthprovider = OAuthProvider.prisma().create(
            data={
                # data to create a OAuthProvider record
                'userId': 'bjcbfcieaa',
                'provider': 'cbaaechiej',
                'providerId': 'iejbeaaeg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.OAuthProviderCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple OAuthProvider records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of OAuthProvider record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = OAuthProvider.prisma().create_many(
            data=[
                {
                    # data to create a OAuthProvider record
                    'userId': 'jcibfcbhf',
                    'provider': 'chdadcaga',
                    'providerId': 'jicieifbh',
                },
                {
                    # data to create a OAuthProvider record
                    'userId': 'fbahdheji',
                    'provider': 'cbbheiicgh',
                    'providerId': 'beabjeejdg',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.OAuthProviderWhereUniqueInput,
        include: Optional[types.OAuthProviderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single OAuthProvider record.

        Parameters
        ----------
        where
            OAuthProvider filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned OAuthProvider model

        Returns
        -------
        prisma.models.OAuthProvider
            The deleted OAuthProvider record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        oauthprovider = OAuthProvider.prisma().delete(
            where={
                'id': 'bcjhgahffd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.OAuthProviderWhereUniqueInput,
        include: Optional[types.OAuthProviderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique OAuthProvider record.

        Parameters
        ----------
        where
            OAuthProvider filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned OAuthProvider model

        Returns
        -------
        prisma.models.OAuthProvider
            The found OAuthProvider record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        oauthprovider = OAuthProvider.prisma().find_unique(
            where={
                'id': 'fbjeiiffa',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.OAuthProviderWhereUniqueInput,
        include: Optional[types.OAuthProviderInclude] = None
    ) -> _PrismaModelT:
        """Find a unique OAuthProvider record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            OAuthProvider filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned OAuthProvider model

        Returns
        -------
        prisma.models.OAuthProvider
            The found OAuthProvider record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        oauthprovider = OAuthProvider.prisma().find_unique_or_raise(
            where={
                'id': 'jhgidcgbf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OAuthProviderWhereInput] = None,
        cursor: Optional[types.OAuthProviderWhereUniqueInput] = None,
        include: Optional[types.OAuthProviderInclude] = None,
        order: Optional[Union[types.OAuthProviderOrderByInput, List[types.OAuthProviderOrderByInput]]] = None,
        distinct: Optional[List[types.OAuthProviderScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple OAuthProvider records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of OAuthProvider records returned
        skip
            Ignore the first N results
        where
            OAuthProvider filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned OAuthProvider model
        order
            Order the returned OAuthProvider records by any field
        distinct
            Filter OAuthProvider records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.OAuthProvider]
            The list of all OAuthProvider records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 OAuthProvider records
        oauthproviders = OAuthProvider.prisma().find_many(take=10)

        # find the first 5 OAuthProvider records ordered by the provider field
        oauthproviders = OAuthProvider.prisma().find_many(
            take=5,
            order={
                'provider': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.OAuthProviderWhereInput] = None,
        cursor: Optional[types.OAuthProviderWhereUniqueInput] = None,
        include: Optional[types.OAuthProviderInclude] = None,
        order: Optional[Union[types.OAuthProviderOrderByInput, List[types.OAuthProviderOrderByInput]]] = None,
        distinct: Optional[List[types.OAuthProviderScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single OAuthProvider record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            OAuthProvider filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned OAuthProvider model
        order
            Order the returned OAuthProvider records by any field
        distinct
            Filter OAuthProvider records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.OAuthProvider
            The first OAuthProvider record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second OAuthProvider record ordered by the providerId field
        oauthprovider = OAuthProvider.prisma().find_first(
            skip=1,
            order={
                'providerId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.OAuthProviderWhereInput] = None,
        cursor: Optional[types.OAuthProviderWhereUniqueInput] = None,
        include: Optional[types.OAuthProviderInclude] = None,
        order: Optional[Union[types.OAuthProviderOrderByInput, List[types.OAuthProviderOrderByInput]]] = None,
        distinct: Optional[List[types.OAuthProviderScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single OAuthProvider record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            OAuthProvider filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned OAuthProvider model
        order
            Order the returned OAuthProvider records by any field
        distinct
            Filter OAuthProvider records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.OAuthProvider
            The first OAuthProvider record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second OAuthProvider record ordered by the createdAt field
        oauthprovider = OAuthProvider.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.OAuthProviderUpdateInput,
        where: types.OAuthProviderWhereUniqueInput,
        include: Optional[types.OAuthProviderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single OAuthProvider record.

        Parameters
        ----------
        data
            OAuthProvider record data specifying what to update
        where
            OAuthProvider filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned OAuthProvider model

        Returns
        -------
        prisma.models.OAuthProvider
            The updated OAuthProvider record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        oauthprovider = OAuthProvider.prisma().update(
            where={
                'id': 'bgjgecfejc',
            },
            data={
                # data to update the OAuthProvider record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.OAuthProviderWhereUniqueInput,
        data: types.OAuthProviderUpsertInput,
        include: Optional[types.OAuthProviderInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            OAuthProvider filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned OAuthProvider model

        Returns
        -------
        prisma.models.OAuthProvider
            The created or updated OAuthProvider record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        oauthprovider = OAuthProvider.prisma().upsert(
            where={
                'id': 'bgjcgchib',
            },
            data={
                'create': {
                    'id': 'bgjcgchib',
                    'userId': 'fbahdheji',
                    'provider': 'cbbheiicgh',
                    'providerId': 'beabjeejdg',
                },
                'update': {
                    'userId': 'fbahdheji',
                    'provider': 'cbbheiicgh',
                    'providerId': 'beabjeejdg',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.OAuthProviderUpdateManyMutationInput,
        where: types.OAuthProviderWhereInput,
    ) -> int:
        """Update multiple OAuthProvider records

        Parameters
        ----------
        data
            OAuthProvider data to update the selected OAuthProvider records to
        where
            Filter to select the OAuthProvider records to update

        Returns
        -------
        int
            The total number of OAuthProvider records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all OAuthProvider records
        total = OAuthProvider.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OAuthProviderWhereInput] = None,
        cursor: Optional[types.OAuthProviderWhereUniqueInput] = None,
    ) -> int:
        """Count the number of OAuthProvider records present in the database

        Parameters
        ----------
        select
            Select the OAuthProvider fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            OAuthProvider filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.OAuthProviderCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = OAuthProvider.prisma().count()

        # results: prisma.types.OAuthProviderCountAggregateOutput
        results = OAuthProvider.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.OAuthProviderCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OAuthProviderWhereInput] = None,
        cursor: Optional[types.OAuthProviderWhereUniqueInput] = None,
    ) -> types.OAuthProviderCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.OAuthProviderCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OAuthProviderWhereInput] = None,
        cursor: Optional[types.OAuthProviderWhereUniqueInput] = None,
    ) -> Union[int, types.OAuthProviderCountAggregateOutput]:
        """Count the number of OAuthProvider records present in the database

        Parameters
        ----------
        select
            Select the OAuthProvider fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            OAuthProvider filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.OAuthProviderCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = OAuthProvider.prisma().count()

        # results: prisma.types.OAuthProviderCountAggregateOutput
        results = OAuthProvider.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.OAuthProviderCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.OAuthProviderWhereInput] = None
    ) -> int:
        """Delete multiple OAuthProvider records.

        Parameters
        ----------
        where
            Optional OAuthProvider filter to find the records to be deleted

        Returns
        -------
        int
            The total number of OAuthProvider records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all OAuthProvider records
        total = OAuthProvider.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.OAuthProviderScalarFieldKeys'],
        *,
        where: Optional['types.OAuthProviderWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.OAuthProviderAvgAggregateInput'] = None,
        sum: Optional['types.OAuthProviderSumAggregateInput'] = None,
        min: Optional['types.OAuthProviderMinAggregateInput'] = None,
        max: Optional['types.OAuthProviderMaxAggregateInput'] = None,
        having: Optional['types.OAuthProviderScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.OAuthProviderCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.OAuthProviderScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.OAuthProviderScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.OAuthProviderGroupByOutput']:
        """Group OAuthProvider records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar OAuthProvider fields to group records by
        where
            OAuthProvider filter to select records
        take
            Limit the maximum number of OAuthProvider records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.OAuthProviderGroupByOutput]
            A list of dictionaries representing the OAuthProvider record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group OAuthProvider records by provider values
        # and count how many records are in each group
        results = OAuthProvider.prisma().group_by(
            ['provider'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserMedicalInformationActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.UserMedicalInformation]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = UserMedicalInformation.prisma().query_raw(
            'SELECT * FROM UserMedicalInformation WHERE id = $1',
            'bacdaibgfa',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.UserMedicalInformation
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = UserMedicalInformation.prisma().query_first(
            'SELECT * FROM UserMedicalInformation WHERE userId = $1',
            'dchgibach',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UserMedicalInformationCreateInput,
        include: Optional[types.UserMedicalInformationInclude] = None
    ) -> _PrismaModelT:
        """Create a new UserMedicalInformation record.

        Parameters
        ----------
        data
            UserMedicalInformation record data
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model

        Returns
        -------
        prisma.models.UserMedicalInformation
            The created UserMedicalInformation record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a UserMedicalInformation record from just the required fields
        usermedicalinformation = UserMedicalInformation.prisma().create(
            data={
                # data to create a UserMedicalInformation record
                'userId': 'fchheijjc',
                'emergencyContactName': 'cacjdfhejh',
                'emergencyContactPhone': 'bdbifjhbbi',
                'emergencyContactRelationship': 'cbccbbcdfb',
                'emergencyContactEmail': 'bacejedaca',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UserMedicalInformationCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple UserMedicalInformation records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of UserMedicalInformation record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = UserMedicalInformation.prisma().create_many(
            data=[
                {
                    # data to create a UserMedicalInformation record
                    'userId': 'bhbhdahfaj',
                    'emergencyContactName': 'bfjibceaec',
                    'emergencyContactPhone': 'ibhgcdbgd',
                    'emergencyContactRelationship': 'badaffhddg',
                    'emergencyContactEmail': 'bbdbfcfihd',
                },
                {
                    # data to create a UserMedicalInformation record
                    'userId': 'cbagggbji',
                    'emergencyContactName': 'bchgafhjed',
                    'emergencyContactPhone': 'heffgjdei',
                    'emergencyContactRelationship': 'dahihgbeb',
                    'emergencyContactEmail': 'bgheaejbcc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UserMedicalInformationWhereUniqueInput,
        include: Optional[types.UserMedicalInformationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single UserMedicalInformation record.

        Parameters
        ----------
        where
            UserMedicalInformation filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model

        Returns
        -------
        prisma.models.UserMedicalInformation
            The deleted UserMedicalInformation record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermedicalinformation = UserMedicalInformation.prisma().delete(
            where={
                'id': 'bfcgifeged',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UserMedicalInformationWhereUniqueInput,
        include: Optional[types.UserMedicalInformationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique UserMedicalInformation record.

        Parameters
        ----------
        where
            UserMedicalInformation filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model

        Returns
        -------
        prisma.models.UserMedicalInformation
            The found UserMedicalInformation record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermedicalinformation = UserMedicalInformation.prisma().find_unique(
            where={
                'id': 'jfiahhbae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UserMedicalInformationWhereUniqueInput,
        include: Optional[types.UserMedicalInformationInclude] = None
    ) -> _PrismaModelT:
        """Find a unique UserMedicalInformation record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            UserMedicalInformation filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model

        Returns
        -------
        prisma.models.UserMedicalInformation
            The found UserMedicalInformation record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermedicalinformation = UserMedicalInformation.prisma().find_unique_or_raise(
            where={
                'id': 'bfbdafajcb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserMedicalInformationWhereInput] = None,
        cursor: Optional[types.UserMedicalInformationWhereUniqueInput] = None,
        include: Optional[types.UserMedicalInformationInclude] = None,
        order: Optional[Union[types.UserMedicalInformationOrderByInput, List[types.UserMedicalInformationOrderByInput]]] = None,
        distinct: Optional[List[types.UserMedicalInformationScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple UserMedicalInformation records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of UserMedicalInformation records returned
        skip
            Ignore the first N results
        where
            UserMedicalInformation filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model
        order
            Order the returned UserMedicalInformation records by any field
        distinct
            Filter UserMedicalInformation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.UserMedicalInformation]
            The list of all UserMedicalInformation records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 UserMedicalInformation records
        usermedicalinformations = UserMedicalInformation.prisma().find_many(take=10)

        # find the first 5 UserMedicalInformation records ordered by the hasPatologies field
        usermedicalinformations = UserMedicalInformation.prisma().find_many(
            take=5,
            order={
                'hasPatologies': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserMedicalInformationWhereInput] = None,
        cursor: Optional[types.UserMedicalInformationWhereUniqueInput] = None,
        include: Optional[types.UserMedicalInformationInclude] = None,
        order: Optional[Union[types.UserMedicalInformationOrderByInput, List[types.UserMedicalInformationOrderByInput]]] = None,
        distinct: Optional[List[types.UserMedicalInformationScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single UserMedicalInformation record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserMedicalInformation filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model
        order
            Order the returned UserMedicalInformation records by any field
        distinct
            Filter UserMedicalInformation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserMedicalInformation
            The first UserMedicalInformation record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserMedicalInformation record ordered by the hasMedication field
        usermedicalinformation = UserMedicalInformation.prisma().find_first(
            skip=1,
            order={
                'hasMedication': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserMedicalInformationWhereInput] = None,
        cursor: Optional[types.UserMedicalInformationWhereUniqueInput] = None,
        include: Optional[types.UserMedicalInformationInclude] = None,
        order: Optional[Union[types.UserMedicalInformationOrderByInput, List[types.UserMedicalInformationOrderByInput]]] = None,
        distinct: Optional[List[types.UserMedicalInformationScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single UserMedicalInformation record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserMedicalInformation filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model
        order
            Order the returned UserMedicalInformation records by any field
        distinct
            Filter UserMedicalInformation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserMedicalInformation
            The first UserMedicalInformation record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserMedicalInformation record ordered by the hasAllergies field
        usermedicalinformation = UserMedicalInformation.prisma().find_first_or_raise(
            skip=1,
            order={
                'hasAllergies': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UserMedicalInformationUpdateInput,
        where: types.UserMedicalInformationWhereUniqueInput,
        include: Optional[types.UserMedicalInformationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single UserMedicalInformation record.

        Parameters
        ----------
        data
            UserMedicalInformation record data specifying what to update
        where
            UserMedicalInformation filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model

        Returns
        -------
        prisma.models.UserMedicalInformation
            The updated UserMedicalInformation record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        usermedicalinformation = UserMedicalInformation.prisma().update(
            where={
                'id': 'caeghehde',
            },
            data={
                # data to update the UserMedicalInformation record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UserMedicalInformationWhereUniqueInput,
        data: types.UserMedicalInformationUpsertInput,
        include: Optional[types.UserMedicalInformationInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            UserMedicalInformation filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned UserMedicalInformation model

        Returns
        -------
        prisma.models.UserMedicalInformation
            The created or updated UserMedicalInformation record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        usermedicalinformation = UserMedicalInformation.prisma().upsert(
            where={
                'id': 'caghgfbggd',
            },
            data={
                'create': {
                    'id': 'caghgfbggd',
                    'userId': 'cbagggbji',
                    'emergencyContactName': 'bchgafhjed',
                    'emergencyContactPhone': 'heffgjdei',
                    'emergencyContactRelationship': 'dahihgbeb',
                    'emergencyContactEmail': 'bgheaejbcc',
                },
                'update': {
                    'userId': 'cbagggbji',
                    'emergencyContactName': 'bchgafhjed',
                    'emergencyContactPhone': 'heffgjdei',
                    'emergencyContactRelationship': 'dahihgbeb',
                    'emergencyContactEmail': 'bgheaejbcc',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UserMedicalInformationUpdateManyMutationInput,
        where: types.UserMedicalInformationWhereInput,
    ) -> int:
        """Update multiple UserMedicalInformation records

        Parameters
        ----------
        data
            UserMedicalInformation data to update the selected UserMedicalInformation records to
        where
            Filter to select the UserMedicalInformation records to update

        Returns
        -------
        int
            The total number of UserMedicalInformation records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all UserMedicalInformation records
        total = UserMedicalInformation.prisma().update_many(
            data={
                'hasMedicationAllergies': True
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserMedicalInformationWhereInput] = None,
        cursor: Optional[types.UserMedicalInformationWhereUniqueInput] = None,
    ) -> int:
        """Count the number of UserMedicalInformation records present in the database

        Parameters
        ----------
        select
            Select the UserMedicalInformation fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserMedicalInformation filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserMedicalInformationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = UserMedicalInformation.prisma().count()

        # results: prisma.types.UserMedicalInformationCountAggregateOutput
        results = UserMedicalInformation.prisma().count(
            select={
                '_all': True,
                'hasSurgeryHistory': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UserMedicalInformationCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserMedicalInformationWhereInput] = None,
        cursor: Optional[types.UserMedicalInformationWhereUniqueInput] = None,
    ) -> types.UserMedicalInformationCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UserMedicalInformationCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserMedicalInformationWhereInput] = None,
        cursor: Optional[types.UserMedicalInformationWhereUniqueInput] = None,
    ) -> Union[int, types.UserMedicalInformationCountAggregateOutput]:
        """Count the number of UserMedicalInformation records present in the database

        Parameters
        ----------
        select
            Select the UserMedicalInformation fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserMedicalInformation filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserMedicalInformationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = UserMedicalInformation.prisma().count()

        # results: prisma.types.UserMedicalInformationCountAggregateOutput
        results = UserMedicalInformation.prisma().count(
            select={
                '_all': True,
                'hasDaietaryRestrictions': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserMedicalInformationCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UserMedicalInformationWhereInput] = None
    ) -> int:
        """Delete multiple UserMedicalInformation records.

        Parameters
        ----------
        where
            Optional UserMedicalInformation filter to find the records to be deleted

        Returns
        -------
        int
            The total number of UserMedicalInformation records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all UserMedicalInformation records
        total = UserMedicalInformation.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UserMedicalInformationScalarFieldKeys'],
        *,
        where: Optional['types.UserMedicalInformationWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserMedicalInformationAvgAggregateInput'] = None,
        sum: Optional['types.UserMedicalInformationSumAggregateInput'] = None,
        min: Optional['types.UserMedicalInformationMinAggregateInput'] = None,
        max: Optional['types.UserMedicalInformationMaxAggregateInput'] = None,
        having: Optional['types.UserMedicalInformationScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserMedicalInformationCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserMedicalInformationScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserMedicalInformationScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserMedicalInformationGroupByOutput']:
        """Group UserMedicalInformation records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar UserMedicalInformation fields to group records by
        where
            UserMedicalInformation filter to select records
        take
            Limit the maximum number of UserMedicalInformation records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserMedicalInformationGroupByOutput]
            A list of dictionaries representing the UserMedicalInformation record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group UserMedicalInformation records by patologies values
        # and count how many records are in each group
        results = UserMedicalInformation.prisma().group_by(
            ['patologies'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class RoleActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Role]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Role.prisma().query_raw(
            'SELECT * FROM Role WHERE id = $1',
            'bfijhaejdd',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Role
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Role.prisma().query_first(
            'SELECT * FROM Role WHERE name = $1',
            'bcedehfiji',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.RoleCreateInput,
        include: Optional[types.RoleInclude] = None
    ) -> _PrismaModelT:
        """Create a new Role record.

        Parameters
        ----------
        data
            Role record data
        include
            Specifies which relations should be loaded on the returned Role model

        Returns
        -------
        prisma.models.Role
            The created Role record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Role record from just the required fields
        role = Role.prisma().create(
            data={
                # data to create a Role record
                'name': 'bdgjicijhb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.RoleCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Role records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Role record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Role.prisma().create_many(
            data=[
                {
                    # data to create a Role record
                    'name': 'bghifjdeia',
                },
                {
                    # data to create a Role record
                    'name': 'eadfcbbcb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.RoleWhereUniqueInput,
        include: Optional[types.RoleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Role record.

        Parameters
        ----------
        where
            Role filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Role model

        Returns
        -------
        prisma.models.Role
            The deleted Role record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        role = Role.prisma().delete(
            where={
                'id': 'geihgahba',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.RoleWhereUniqueInput,
        include: Optional[types.RoleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Role record.

        Parameters
        ----------
        where
            Role filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Role model

        Returns
        -------
        prisma.models.Role
            The found Role record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        role = Role.prisma().find_unique(
            where={
                'id': 'gahdcdhbj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.RoleWhereUniqueInput,
        include: Optional[types.RoleInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Role record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Role filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Role model

        Returns
        -------
        prisma.models.Role
            The found Role record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        role = Role.prisma().find_unique_or_raise(
            where={
                'id': 'begiijahea',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RoleWhereInput] = None,
        cursor: Optional[types.RoleWhereUniqueInput] = None,
        include: Optional[types.RoleInclude] = None,
        order: Optional[Union[types.RoleOrderByInput, List[types.RoleOrderByInput]]] = None,
        distinct: Optional[List[types.RoleScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Role records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Role records returned
        skip
            Ignore the first N results
        where
            Role filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Role model
        order
            Order the returned Role records by any field
        distinct
            Filter Role records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Role]
            The list of all Role records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Role records
        roles = Role.prisma().find_many(take=10)

        # find the first 5 Role records ordered by the description field
        roles = Role.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RoleWhereInput] = None,
        cursor: Optional[types.RoleWhereUniqueInput] = None,
        include: Optional[types.RoleInclude] = None,
        order: Optional[Union[types.RoleOrderByInput, List[types.RoleOrderByInput]]] = None,
        distinct: Optional[List[types.RoleScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Role record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Role filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Role model
        order
            Order the returned Role records by any field
        distinct
            Filter Role records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Role
            The first Role record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Role record ordered by the createdAt field
        role = Role.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RoleWhereInput] = None,
        cursor: Optional[types.RoleWhereUniqueInput] = None,
        include: Optional[types.RoleInclude] = None,
        order: Optional[Union[types.RoleOrderByInput, List[types.RoleOrderByInput]]] = None,
        distinct: Optional[List[types.RoleScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Role record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Role filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Role model
        order
            Order the returned Role records by any field
        distinct
            Filter Role records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Role
            The first Role record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Role record ordered by the updatedAt field
        role = Role.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.RoleUpdateInput,
        where: types.RoleWhereUniqueInput,
        include: Optional[types.RoleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Role record.

        Parameters
        ----------
        data
            Role record data specifying what to update
        where
            Role filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Role model

        Returns
        -------
        prisma.models.Role
            The updated Role record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        role = Role.prisma().update(
            where={
                'id': 'gcjadjaaf',
            },
            data={
                # data to update the Role record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.RoleWhereUniqueInput,
        data: types.RoleUpsertInput,
        include: Optional[types.RoleInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Role filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Role model

        Returns
        -------
        prisma.models.Role
            The created or updated Role record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        role = Role.prisma().upsert(
            where={
                'id': 'bcbebgiaic',
            },
            data={
                'create': {
                    'id': 'bcbebgiaic',
                    'name': 'eadfcbbcb',
                },
                'update': {
                    'name': 'eadfcbbcb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.RoleUpdateManyMutationInput,
        where: types.RoleWhereInput,
    ) -> int:
        """Update multiple Role records

        Parameters
        ----------
        data
            Role data to update the selected Role records to
        where
            Filter to select the Role records to update

        Returns
        -------
        int
            The total number of Role records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Role records
        total = Role.prisma().update_many(
            data={
                'id': 'ijigbdcbj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RoleWhereInput] = None,
        cursor: Optional[types.RoleWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Role records present in the database

        Parameters
        ----------
        select
            Select the Role fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Role filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RoleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Role.prisma().count()

        # results: prisma.types.RoleCountAggregateOutput
        results = Role.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.RoleCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RoleWhereInput] = None,
        cursor: Optional[types.RoleWhereUniqueInput] = None,
    ) -> types.RoleCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.RoleCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RoleWhereInput] = None,
        cursor: Optional[types.RoleWhereUniqueInput] = None,
    ) -> Union[int, types.RoleCountAggregateOutput]:
        """Count the number of Role records present in the database

        Parameters
        ----------
        select
            Select the Role fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Role filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RoleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Role.prisma().count()

        # results: prisma.types.RoleCountAggregateOutput
        results = Role.prisma().count(
            select={
                '_all': True,
                'description': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RoleCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.RoleWhereInput] = None
    ) -> int:
        """Delete multiple Role records.

        Parameters
        ----------
        where
            Optional Role filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Role records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Role records
        total = Role.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.RoleScalarFieldKeys'],
        *,
        where: Optional['types.RoleWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RoleAvgAggregateInput'] = None,
        sum: Optional['types.RoleSumAggregateInput'] = None,
        min: Optional['types.RoleMinAggregateInput'] = None,
        max: Optional['types.RoleMaxAggregateInput'] = None,
        having: Optional['types.RoleScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RoleCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RoleScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RoleScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RoleGroupByOutput']:
        """Group Role records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Role fields to group records by
        where
            Role filter to select records
        take
            Limit the maximum number of Role records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RoleGroupByOutput]
            A list of dictionaries representing the Role record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Role records by createdAt values
        # and count how many records are in each group
        results = Role.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PermissionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Permission]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Permission.prisma().query_raw(
            'SELECT * FROM Permission WHERE id = $1',
            'gfidhicai',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Permission
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Permission.prisma().query_first(
            'SELECT * FROM Permission WHERE module = $1',
            'jfegcaafh',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PermissionCreateInput,
        include: Optional[types.PermissionInclude] = None
    ) -> _PrismaModelT:
        """Create a new Permission record.

        Parameters
        ----------
        data
            Permission record data
        include
            Specifies which relations should be loaded on the returned Permission model

        Returns
        -------
        prisma.models.Permission
            The created Permission record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Permission record from just the required fields
        permission = Permission.prisma().create(
            data={
                # data to create a Permission record
                'module': 'bcbeiajjfa',
                'action': 'baehicaajf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PermissionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Permission records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Permission record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Permission.prisma().create_many(
            data=[
                {
                    # data to create a Permission record
                    'module': 'bdachdeiga',
                    'action': 'ijdafccef',
                },
                {
                    # data to create a Permission record
                    'module': 'ciaaiddag',
                    'action': 'fejggijff',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PermissionWhereUniqueInput,
        include: Optional[types.PermissionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Permission record.

        Parameters
        ----------
        where
            Permission filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Permission model

        Returns
        -------
        prisma.models.Permission
            The deleted Permission record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permission = Permission.prisma().delete(
            where={
                'id': 'hghjaaai',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PermissionWhereUniqueInput,
        include: Optional[types.PermissionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Permission record.

        Parameters
        ----------
        where
            Permission filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Permission model

        Returns
        -------
        prisma.models.Permission
            The found Permission record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permission = Permission.prisma().find_unique(
            where={
                'id': 'cajicjjdef',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PermissionWhereUniqueInput,
        include: Optional[types.PermissionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Permission record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Permission filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Permission model

        Returns
        -------
        prisma.models.Permission
            The found Permission record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permission = Permission.prisma().find_unique_or_raise(
            where={
                'id': 'cefjaadec',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionWhereInput] = None,
        cursor: Optional[types.PermissionWhereUniqueInput] = None,
        include: Optional[types.PermissionInclude] = None,
        order: Optional[Union[types.PermissionOrderByInput, List[types.PermissionOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Permission records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Permission records returned
        skip
            Ignore the first N results
        where
            Permission filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Permission model
        order
            Order the returned Permission records by any field
        distinct
            Filter Permission records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Permission]
            The list of all Permission records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Permission records
        permissions = Permission.prisma().find_many(take=10)

        # find the first 5 Permission records ordered by the action field
        permissions = Permission.prisma().find_many(
            take=5,
            order={
                'action': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PermissionWhereInput] = None,
        cursor: Optional[types.PermissionWhereUniqueInput] = None,
        include: Optional[types.PermissionInclude] = None,
        order: Optional[Union[types.PermissionOrderByInput, List[types.PermissionOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Permission record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Permission filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Permission model
        order
            Order the returned Permission records by any field
        distinct
            Filter Permission records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Permission
            The first Permission record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Permission record ordered by the description field
        permission = Permission.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PermissionWhereInput] = None,
        cursor: Optional[types.PermissionWhereUniqueInput] = None,
        include: Optional[types.PermissionInclude] = None,
        order: Optional[Union[types.PermissionOrderByInput, List[types.PermissionOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Permission record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Permission filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Permission model
        order
            Order the returned Permission records by any field
        distinct
            Filter Permission records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Permission
            The first Permission record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Permission record ordered by the createdAt field
        permission = Permission.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PermissionUpdateInput,
        where: types.PermissionWhereUniqueInput,
        include: Optional[types.PermissionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Permission record.

        Parameters
        ----------
        data
            Permission record data specifying what to update
        where
            Permission filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Permission model

        Returns
        -------
        prisma.models.Permission
            The updated Permission record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        permission = Permission.prisma().update(
            where={
                'id': 'ibbigdigd',
            },
            data={
                # data to update the Permission record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PermissionWhereUniqueInput,
        data: types.PermissionUpsertInput,
        include: Optional[types.PermissionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Permission filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Permission model

        Returns
        -------
        prisma.models.Permission
            The created or updated Permission record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permission = Permission.prisma().upsert(
            where={
                'id': 'bdiiiabbii',
            },
            data={
                'create': {
                    'id': 'bdiiiabbii',
                    'module': 'ciaaiddag',
                    'action': 'fejggijff',
                },
                'update': {
                    'module': 'ciaaiddag',
                    'action': 'fejggijff',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PermissionUpdateManyMutationInput,
        where: types.PermissionWhereInput,
    ) -> int:
        """Update multiple Permission records

        Parameters
        ----------
        data
            Permission data to update the selected Permission records to
        where
            Filter to select the Permission records to update

        Returns
        -------
        int
            The total number of Permission records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Permission records
        total = Permission.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionWhereInput] = None,
        cursor: Optional[types.PermissionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Permission records present in the database

        Parameters
        ----------
        select
            Select the Permission fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Permission filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PermissionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Permission.prisma().count()

        # results: prisma.types.PermissionCountAggregateOutput
        results = Permission.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PermissionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionWhereInput] = None,
        cursor: Optional[types.PermissionWhereUniqueInput] = None,
    ) -> types.PermissionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PermissionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionWhereInput] = None,
        cursor: Optional[types.PermissionWhereUniqueInput] = None,
    ) -> Union[int, types.PermissionCountAggregateOutput]:
        """Count the number of Permission records present in the database

        Parameters
        ----------
        select
            Select the Permission fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Permission filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PermissionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Permission.prisma().count()

        # results: prisma.types.PermissionCountAggregateOutput
        results = Permission.prisma().count(
            select={
                '_all': True,
                'module': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PermissionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PermissionWhereInput] = None
    ) -> int:
        """Delete multiple Permission records.

        Parameters
        ----------
        where
            Optional Permission filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Permission records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Permission records
        total = Permission.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PermissionScalarFieldKeys'],
        *,
        where: Optional['types.PermissionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PermissionAvgAggregateInput'] = None,
        sum: Optional['types.PermissionSumAggregateInput'] = None,
        min: Optional['types.PermissionMinAggregateInput'] = None,
        max: Optional['types.PermissionMaxAggregateInput'] = None,
        having: Optional['types.PermissionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PermissionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PermissionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PermissionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PermissionGroupByOutput']:
        """Group Permission records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Permission fields to group records by
        where
            Permission filter to select records
        take
            Limit the maximum number of Permission records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PermissionGroupByOutput]
            A list of dictionaries representing the Permission record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Permission records by action values
        # and count how many records are in each group
        results = Permission.prisma().group_by(
            ['action'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class RolePermissionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.RolePermission]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = RolePermission.prisma().query_raw(
            'SELECT * FROM RolePermission WHERE id = $1',
            'hfcfhhadh',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.RolePermission
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = RolePermission.prisma().query_first(
            'SELECT * FROM RolePermission WHERE roleId = $1',
            'bbihggdcji',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.RolePermissionCreateInput,
        include: Optional[types.RolePermissionInclude] = None
    ) -> _PrismaModelT:
        """Create a new RolePermission record.

        Parameters
        ----------
        data
            RolePermission record data
        include
            Specifies which relations should be loaded on the returned RolePermission model

        Returns
        -------
        prisma.models.RolePermission
            The created RolePermission record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a RolePermission record from just the required fields
        rolepermission = RolePermission.prisma().create(
            data={
                # data to create a RolePermission record
                'roleId': 'hgjgibdgd',
                'permissionId': 'bcbecjfice',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.RolePermissionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple RolePermission records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of RolePermission record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = RolePermission.prisma().create_many(
            data=[
                {
                    # data to create a RolePermission record
                    'roleId': 'bacbebhjjd',
                    'permissionId': 'dfbfaddhe',
                },
                {
                    # data to create a RolePermission record
                    'roleId': 'bdcbbieibf',
                    'permissionId': 'dgjhdcggi',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.RolePermissionWhereUniqueInput,
        include: Optional[types.RolePermissionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single RolePermission record.

        Parameters
        ----------
        where
            RolePermission filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned RolePermission model

        Returns
        -------
        prisma.models.RolePermission
            The deleted RolePermission record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        rolepermission = RolePermission.prisma().delete(
            where={
                'id': 'bbjbcdfabd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.RolePermissionWhereUniqueInput,
        include: Optional[types.RolePermissionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique RolePermission record.

        Parameters
        ----------
        where
            RolePermission filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned RolePermission model

        Returns
        -------
        prisma.models.RolePermission
            The found RolePermission record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        rolepermission = RolePermission.prisma().find_unique(
            where={
                'id': 'gchfgbcec',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.RolePermissionWhereUniqueInput,
        include: Optional[types.RolePermissionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique RolePermission record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            RolePermission filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned RolePermission model

        Returns
        -------
        prisma.models.RolePermission
            The found RolePermission record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        rolepermission = RolePermission.prisma().find_unique_or_raise(
            where={
                'id': 'bihcjfcjah',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RolePermissionWhereInput] = None,
        cursor: Optional[types.RolePermissionWhereUniqueInput] = None,
        include: Optional[types.RolePermissionInclude] = None,
        order: Optional[Union[types.RolePermissionOrderByInput, List[types.RolePermissionOrderByInput]]] = None,
        distinct: Optional[List[types.RolePermissionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple RolePermission records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of RolePermission records returned
        skip
            Ignore the first N results
        where
            RolePermission filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RolePermission model
        order
            Order the returned RolePermission records by any field
        distinct
            Filter RolePermission records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.RolePermission]
            The list of all RolePermission records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 RolePermission records
        rolepermissions = RolePermission.prisma().find_many(take=10)

        # find the first 5 RolePermission records ordered by the permissionId field
        rolepermissions = RolePermission.prisma().find_many(
            take=5,
            order={
                'permissionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RolePermissionWhereInput] = None,
        cursor: Optional[types.RolePermissionWhereUniqueInput] = None,
        include: Optional[types.RolePermissionInclude] = None,
        order: Optional[Union[types.RolePermissionOrderByInput, List[types.RolePermissionOrderByInput]]] = None,
        distinct: Optional[List[types.RolePermissionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single RolePermission record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            RolePermission filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RolePermission model
        order
            Order the returned RolePermission records by any field
        distinct
            Filter RolePermission records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.RolePermission
            The first RolePermission record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second RolePermission record ordered by the createdAt field
        rolepermission = RolePermission.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RolePermissionWhereInput] = None,
        cursor: Optional[types.RolePermissionWhereUniqueInput] = None,
        include: Optional[types.RolePermissionInclude] = None,
        order: Optional[Union[types.RolePermissionOrderByInput, List[types.RolePermissionOrderByInput]]] = None,
        distinct: Optional[List[types.RolePermissionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single RolePermission record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            RolePermission filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RolePermission model
        order
            Order the returned RolePermission records by any field
        distinct
            Filter RolePermission records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.RolePermission
            The first RolePermission record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second RolePermission record ordered by the id field
        rolepermission = RolePermission.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.RolePermissionUpdateInput,
        where: types.RolePermissionWhereUniqueInput,
        include: Optional[types.RolePermissionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single RolePermission record.

        Parameters
        ----------
        data
            RolePermission record data specifying what to update
        where
            RolePermission filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned RolePermission model

        Returns
        -------
        prisma.models.RolePermission
            The updated RolePermission record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        rolepermission = RolePermission.prisma().update(
            where={
                'id': 'bhjdcicaii',
            },
            data={
                # data to update the RolePermission record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.RolePermissionWhereUniqueInput,
        data: types.RolePermissionUpsertInput,
        include: Optional[types.RolePermissionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            RolePermission filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned RolePermission model

        Returns
        -------
        prisma.models.RolePermission
            The created or updated RolePermission record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        rolepermission = RolePermission.prisma().upsert(
            where={
                'id': 'bibedjhcej',
            },
            data={
                'create': {
                    'id': 'bibedjhcej',
                    'roleId': 'bdcbbieibf',
                    'permissionId': 'dgjhdcggi',
                },
                'update': {
                    'roleId': 'bdcbbieibf',
                    'permissionId': 'dgjhdcggi',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.RolePermissionUpdateManyMutationInput,
        where: types.RolePermissionWhereInput,
    ) -> int:
        """Update multiple RolePermission records

        Parameters
        ----------
        data
            RolePermission data to update the selected RolePermission records to
        where
            Filter to select the RolePermission records to update

        Returns
        -------
        int
            The total number of RolePermission records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all RolePermission records
        total = RolePermission.prisma().update_many(
            data={
                'roleId': 'bjcdajabfa'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RolePermissionWhereInput] = None,
        cursor: Optional[types.RolePermissionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of RolePermission records present in the database

        Parameters
        ----------
        select
            Select the RolePermission fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            RolePermission filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RolePermissionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = RolePermission.prisma().count()

        # results: prisma.types.RolePermissionCountAggregateOutput
        results = RolePermission.prisma().count(
            select={
                '_all': True,
                'permissionId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.RolePermissionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RolePermissionWhereInput] = None,
        cursor: Optional[types.RolePermissionWhereUniqueInput] = None,
    ) -> types.RolePermissionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.RolePermissionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RolePermissionWhereInput] = None,
        cursor: Optional[types.RolePermissionWhereUniqueInput] = None,
    ) -> Union[int, types.RolePermissionCountAggregateOutput]:
        """Count the number of RolePermission records present in the database

        Parameters
        ----------
        select
            Select the RolePermission fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            RolePermission filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RolePermissionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = RolePermission.prisma().count()

        # results: prisma.types.RolePermissionCountAggregateOutput
        results = RolePermission.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RolePermissionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.RolePermissionWhereInput] = None
    ) -> int:
        """Delete multiple RolePermission records.

        Parameters
        ----------
        where
            Optional RolePermission filter to find the records to be deleted

        Returns
        -------
        int
            The total number of RolePermission records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all RolePermission records
        total = RolePermission.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.RolePermissionScalarFieldKeys'],
        *,
        where: Optional['types.RolePermissionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RolePermissionAvgAggregateInput'] = None,
        sum: Optional['types.RolePermissionSumAggregateInput'] = None,
        min: Optional['types.RolePermissionMinAggregateInput'] = None,
        max: Optional['types.RolePermissionMaxAggregateInput'] = None,
        having: Optional['types.RolePermissionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RolePermissionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RolePermissionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RolePermissionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RolePermissionGroupByOutput']:
        """Group RolePermission records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar RolePermission fields to group records by
        where
            RolePermission filter to select records
        take
            Limit the maximum number of RolePermission records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RolePermissionGroupByOutput]
            A list of dictionaries representing the RolePermission record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group RolePermission records by id values
        # and count how many records are in each group
        results = RolePermission.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserRoleActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.UserRole]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = UserRole.prisma().query_raw(
            'SELECT * FROM UserRole WHERE id = $1',
            'bchhceeeff',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.UserRole
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = UserRole.prisma().query_first(
            'SELECT * FROM UserRole WHERE userId = $1',
            'bbgaifhdaa',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UserRoleCreateInput,
        include: Optional[types.UserRoleInclude] = None
    ) -> _PrismaModelT:
        """Create a new UserRole record.

        Parameters
        ----------
        data
            UserRole record data
        include
            Specifies which relations should be loaded on the returned UserRole model

        Returns
        -------
        prisma.models.UserRole
            The created UserRole record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a UserRole record from just the required fields
        userrole = UserRole.prisma().create(
            data={
                # data to create a UserRole record
                'userId': 'dgbcdaegb',
                'roleId': 'beagfbbjig',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UserRoleCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple UserRole records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of UserRole record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = UserRole.prisma().create_many(
            data=[
                {
                    # data to create a UserRole record
                    'userId': 'beicihhijb',
                    'roleId': 'fgggcdcjg',
                },
                {
                    # data to create a UserRole record
                    'userId': 'ccjbbjigf',
                    'roleId': 'bhfaabbaha',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UserRoleWhereUniqueInput,
        include: Optional[types.UserRoleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single UserRole record.

        Parameters
        ----------
        where
            UserRole filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned UserRole model

        Returns
        -------
        prisma.models.UserRole
            The deleted UserRole record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userrole = UserRole.prisma().delete(
            where={
                'id': 'ebajedhhf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UserRoleWhereUniqueInput,
        include: Optional[types.UserRoleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique UserRole record.

        Parameters
        ----------
        where
            UserRole filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserRole model

        Returns
        -------
        prisma.models.UserRole
            The found UserRole record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userrole = UserRole.prisma().find_unique(
            where={
                'id': 'jajacedge',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UserRoleWhereUniqueInput,
        include: Optional[types.UserRoleInclude] = None
    ) -> _PrismaModelT:
        """Find a unique UserRole record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            UserRole filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserRole model

        Returns
        -------
        prisma.models.UserRole
            The found UserRole record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userrole = UserRole.prisma().find_unique_or_raise(
            where={
                'id': 'hffgbabgf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserRoleWhereInput] = None,
        cursor: Optional[types.UserRoleWhereUniqueInput] = None,
        include: Optional[types.UserRoleInclude] = None,
        order: Optional[Union[types.UserRoleOrderByInput, List[types.UserRoleOrderByInput]]] = None,
        distinct: Optional[List[types.UserRoleScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple UserRole records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of UserRole records returned
        skip
            Ignore the first N results
        where
            UserRole filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserRole model
        order
            Order the returned UserRole records by any field
        distinct
            Filter UserRole records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.UserRole]
            The list of all UserRole records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 UserRole records
        userroles = UserRole.prisma().find_many(take=10)

        # find the first 5 UserRole records ordered by the roleId field
        userroles = UserRole.prisma().find_many(
            take=5,
            order={
                'roleId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserRoleWhereInput] = None,
        cursor: Optional[types.UserRoleWhereUniqueInput] = None,
        include: Optional[types.UserRoleInclude] = None,
        order: Optional[Union[types.UserRoleOrderByInput, List[types.UserRoleOrderByInput]]] = None,
        distinct: Optional[List[types.UserRoleScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single UserRole record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserRole filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserRole model
        order
            Order the returned UserRole records by any field
        distinct
            Filter UserRole records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserRole
            The first UserRole record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserRole record ordered by the eventId field
        userrole = UserRole.prisma().find_first(
            skip=1,
            order={
                'eventId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserRoleWhereInput] = None,
        cursor: Optional[types.UserRoleWhereUniqueInput] = None,
        include: Optional[types.UserRoleInclude] = None,
        order: Optional[Union[types.UserRoleOrderByInput, List[types.UserRoleOrderByInput]]] = None,
        distinct: Optional[List[types.UserRoleScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single UserRole record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserRole filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserRole model
        order
            Order the returned UserRole records by any field
        distinct
            Filter UserRole records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserRole
            The first UserRole record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserRole record ordered by the assignedAt field
        userrole = UserRole.prisma().find_first_or_raise(
            skip=1,
            order={
                'assignedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UserRoleUpdateInput,
        where: types.UserRoleWhereUniqueInput,
        include: Optional[types.UserRoleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single UserRole record.

        Parameters
        ----------
        data
            UserRole record data specifying what to update
        where
            UserRole filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned UserRole model

        Returns
        -------
        prisma.models.UserRole
            The updated UserRole record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        userrole = UserRole.prisma().update(
            where={
                'id': 'biacbiieja',
            },
            data={
                # data to update the UserRole record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UserRoleWhereUniqueInput,
        data: types.UserRoleUpsertInput,
        include: Optional[types.UserRoleInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            UserRole filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned UserRole model

        Returns
        -------
        prisma.models.UserRole
            The created or updated UserRole record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userrole = UserRole.prisma().upsert(
            where={
                'id': 'cjejbgbff',
            },
            data={
                'create': {
                    'id': 'cjejbgbff',
                    'userId': 'ccjbbjigf',
                    'roleId': 'bhfaabbaha',
                },
                'update': {
                    'userId': 'ccjbbjigf',
                    'roleId': 'bhfaabbaha',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UserRoleUpdateManyMutationInput,
        where: types.UserRoleWhereInput,
    ) -> int:
        """Update multiple UserRole records

        Parameters
        ----------
        data
            UserRole data to update the selected UserRole records to
        where
            Filter to select the UserRole records to update

        Returns
        -------
        int
            The total number of UserRole records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all UserRole records
        total = UserRole.prisma().update_many(
            data={
                'id': 'fgeahddae'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserRoleWhereInput] = None,
        cursor: Optional[types.UserRoleWhereUniqueInput] = None,
    ) -> int:
        """Count the number of UserRole records present in the database

        Parameters
        ----------
        select
            Select the UserRole fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserRole filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserRoleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = UserRole.prisma().count()

        # results: prisma.types.UserRoleCountAggregateOutput
        results = UserRole.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UserRoleCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserRoleWhereInput] = None,
        cursor: Optional[types.UserRoleWhereUniqueInput] = None,
    ) -> types.UserRoleCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UserRoleCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserRoleWhereInput] = None,
        cursor: Optional[types.UserRoleWhereUniqueInput] = None,
    ) -> Union[int, types.UserRoleCountAggregateOutput]:
        """Count the number of UserRole records present in the database

        Parameters
        ----------
        select
            Select the UserRole fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserRole filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserRoleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = UserRole.prisma().count()

        # results: prisma.types.UserRoleCountAggregateOutput
        results = UserRole.prisma().count(
            select={
                '_all': True,
                'roleId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserRoleCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UserRoleWhereInput] = None
    ) -> int:
        """Delete multiple UserRole records.

        Parameters
        ----------
        where
            Optional UserRole filter to find the records to be deleted

        Returns
        -------
        int
            The total number of UserRole records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all UserRole records
        total = UserRole.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UserRoleScalarFieldKeys'],
        *,
        where: Optional['types.UserRoleWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserRoleAvgAggregateInput'] = None,
        sum: Optional['types.UserRoleSumAggregateInput'] = None,
        min: Optional['types.UserRoleMinAggregateInput'] = None,
        max: Optional['types.UserRoleMaxAggregateInput'] = None,
        having: Optional['types.UserRoleScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserRoleCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserRoleScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserRoleScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserRoleGroupByOutput']:
        """Group UserRole records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar UserRole fields to group records by
        where
            UserRole filter to select records
        take
            Limit the maximum number of UserRole records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserRoleGroupByOutput]
            A list of dictionaries representing the UserRole record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group UserRole records by eventId values
        # and count how many records are in each group
        results = UserRole.prisma().group_by(
            ['eventId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class OrganizationActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Organization]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Organization.prisma().query_raw(
            'SELECT * FROM Organization WHERE id = $1',
            'diageigcf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Organization
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Organization.prisma().query_first(
            'SELECT * FROM Organization WHERE name = $1',
            'badagbgeha',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.OrganizationCreateInput,
        include: Optional[types.OrganizationInclude] = None
    ) -> _PrismaModelT:
        """Create a new Organization record.

        Parameters
        ----------
        data
            Organization record data
        include
            Specifies which relations should be loaded on the returned Organization model

        Returns
        -------
        prisma.models.Organization
            The created Organization record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Organization record from just the required fields
        organization = Organization.prisma().create(
            data={
                # data to create a Organization record
                'name': 'ibgebbjch',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.OrganizationCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Organization records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Organization record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Organization.prisma().create_many(
            data=[
                {
                    # data to create a Organization record
                    'name': 'baieajjiee',
                },
                {
                    # data to create a Organization record
                    'name': 'bahjhaccfd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.OrganizationWhereUniqueInput,
        include: Optional[types.OrganizationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Organization record.

        Parameters
        ----------
        where
            Organization filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Organization model

        Returns
        -------
        prisma.models.Organization
            The deleted Organization record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organization = Organization.prisma().delete(
            where={
                'id': 'hffhfabhi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.OrganizationWhereUniqueInput,
        include: Optional[types.OrganizationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Organization record.

        Parameters
        ----------
        where
            Organization filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Organization model

        Returns
        -------
        prisma.models.Organization
            The found Organization record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organization = Organization.prisma().find_unique(
            where={
                'id': 'bbcigiadhb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.OrganizationWhereUniqueInput,
        include: Optional[types.OrganizationInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Organization record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Organization filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Organization model

        Returns
        -------
        prisma.models.Organization
            The found Organization record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organization = Organization.prisma().find_unique_or_raise(
            where={
                'id': 'cfjagbbae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationWhereInput] = None,
        cursor: Optional[types.OrganizationWhereUniqueInput] = None,
        include: Optional[types.OrganizationInclude] = None,
        order: Optional[Union[types.OrganizationOrderByInput, List[types.OrganizationOrderByInput]]] = None,
        distinct: Optional[List[types.OrganizationScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Organization records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Organization records returned
        skip
            Ignore the first N results
        where
            Organization filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Organization model
        order
            Order the returned Organization records by any field
        distinct
            Filter Organization records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Organization]
            The list of all Organization records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Organization records
        organizations = Organization.prisma().find_many(take=10)

        # find the first 5 Organization records ordered by the description field
        organizations = Organization.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationWhereInput] = None,
        cursor: Optional[types.OrganizationWhereUniqueInput] = None,
        include: Optional[types.OrganizationInclude] = None,
        order: Optional[Union[types.OrganizationOrderByInput, List[types.OrganizationOrderByInput]]] = None,
        distinct: Optional[List[types.OrganizationScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Organization record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Organization filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Organization model
        order
            Order the returned Organization records by any field
        distinct
            Filter Organization records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Organization
            The first Organization record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Organization record ordered by the logoUrl field
        organization = Organization.prisma().find_first(
            skip=1,
            order={
                'logoUrl': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationWhereInput] = None,
        cursor: Optional[types.OrganizationWhereUniqueInput] = None,
        include: Optional[types.OrganizationInclude] = None,
        order: Optional[Union[types.OrganizationOrderByInput, List[types.OrganizationOrderByInput]]] = None,
        distinct: Optional[List[types.OrganizationScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Organization record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Organization filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Organization model
        order
            Order the returned Organization records by any field
        distinct
            Filter Organization records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Organization
            The first Organization record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Organization record ordered by the createdAt field
        organization = Organization.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.OrganizationUpdateInput,
        where: types.OrganizationWhereUniqueInput,
        include: Optional[types.OrganizationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Organization record.

        Parameters
        ----------
        data
            Organization record data specifying what to update
        where
            Organization filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Organization model

        Returns
        -------
        prisma.models.Organization
            The updated Organization record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        organization = Organization.prisma().update(
            where={
                'id': 'bbbfhdidef',
            },
            data={
                # data to update the Organization record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.OrganizationWhereUniqueInput,
        data: types.OrganizationUpsertInput,
        include: Optional[types.OrganizationInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Organization filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Organization model

        Returns
        -------
        prisma.models.Organization
            The created or updated Organization record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organization = Organization.prisma().upsert(
            where={
                'id': 'bdadhibhec',
            },
            data={
                'create': {
                    'id': 'bdadhibhec',
                    'name': 'bahjhaccfd',
                },
                'update': {
                    'name': 'bahjhaccfd',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.OrganizationUpdateManyMutationInput,
        where: types.OrganizationWhereInput,
    ) -> int:
        """Update multiple Organization records

        Parameters
        ----------
        data
            Organization data to update the selected Organization records to
        where
            Filter to select the Organization records to update

        Returns
        -------
        int
            The total number of Organization records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Organization records
        total = Organization.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationWhereInput] = None,
        cursor: Optional[types.OrganizationWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Organization records present in the database

        Parameters
        ----------
        select
            Select the Organization fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Organization filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.OrganizationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Organization.prisma().count()

        # results: prisma.types.OrganizationCountAggregateOutput
        results = Organization.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.OrganizationCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationWhereInput] = None,
        cursor: Optional[types.OrganizationWhereUniqueInput] = None,
    ) -> types.OrganizationCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.OrganizationCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationWhereInput] = None,
        cursor: Optional[types.OrganizationWhereUniqueInput] = None,
    ) -> Union[int, types.OrganizationCountAggregateOutput]:
        """Count the number of Organization records present in the database

        Parameters
        ----------
        select
            Select the Organization fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Organization filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.OrganizationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Organization.prisma().count()

        # results: prisma.types.OrganizationCountAggregateOutput
        results = Organization.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.OrganizationCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.OrganizationWhereInput] = None
    ) -> int:
        """Delete multiple Organization records.

        Parameters
        ----------
        where
            Optional Organization filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Organization records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Organization records
        total = Organization.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.OrganizationScalarFieldKeys'],
        *,
        where: Optional['types.OrganizationWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.OrganizationAvgAggregateInput'] = None,
        sum: Optional['types.OrganizationSumAggregateInput'] = None,
        min: Optional['types.OrganizationMinAggregateInput'] = None,
        max: Optional['types.OrganizationMaxAggregateInput'] = None,
        having: Optional['types.OrganizationScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.OrganizationCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.OrganizationScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.OrganizationScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.OrganizationGroupByOutput']:
        """Group Organization records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Organization fields to group records by
        where
            Organization filter to select records
        take
            Limit the maximum number of Organization records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.OrganizationGroupByOutput]
            A list of dictionaries representing the Organization record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Organization records by description values
        # and count how many records are in each group
        results = Organization.prisma().group_by(
            ['description'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class EventActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Event]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Event.prisma().query_raw(
            'SELECT * FROM Event WHERE id = $1',
            'bfhdjaiejf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Event
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Event.prisma().query_first(
            'SELECT * FROM Event WHERE organizationId = $1',
            'bbjfijjadg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.EventCreateInput,
        include: Optional[types.EventInclude] = None
    ) -> _PrismaModelT:
        """Create a new Event record.

        Parameters
        ----------
        data
            Event record data
        include
            Specifies which relations should be loaded on the returned Event model

        Returns
        -------
        prisma.models.Event
            The created Event record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Event record from just the required fields
        event = Event.prisma().create(
            data={
                # data to create a Event record
                'organizationId': 'hdjacbehh',
                'title': 'bhcccbeaba',
                'slug': 'bcgjbdgjdj',
                'description': 'fhdbhifae',
                'startDate': datetime.datetime.utcnow(),
                'endDate': datetime.datetime.utcnow(),
                'countryId': 'beeacgfcej',
                'stateId': 'bbifhdiicc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.EventCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Event records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Event record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Event.prisma().create_many(
            data=[
                {
                    # data to create a Event record
                    'organizationId': 'bgjeccejad',
                    'title': 'bjagdgabbg',
                    'slug': 'bjbbcffdij',
                    'description': 'begcgchdi',
                    'startDate': datetime.datetime.utcnow(),
                    'endDate': datetime.datetime.utcnow(),
                    'countryId': 'bhbjceagbb',
                    'stateId': 'bjeifffjdg',
                },
                {
                    # data to create a Event record
                    'organizationId': 'bdidcfdfjd',
                    'title': 'dfeggejja',
                    'slug': 'gehbgghbj',
                    'description': 'dfhaijeie',
                    'startDate': datetime.datetime.utcnow(),
                    'endDate': datetime.datetime.utcnow(),
                    'countryId': 'gbcdjgicb',
                    'stateId': 'biaibdagac',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.EventWhereUniqueInput,
        include: Optional[types.EventInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Event record.

        Parameters
        ----------
        where
            Event filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Event model

        Returns
        -------
        prisma.models.Event
            The deleted Event record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        event = Event.prisma().delete(
            where={
                'id': 'bbfbheibcd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.EventWhereUniqueInput,
        include: Optional[types.EventInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Event record.

        Parameters
        ----------
        where
            Event filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Event model

        Returns
        -------
        prisma.models.Event
            The found Event record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        event = Event.prisma().find_unique(
            where={
                'id': 'hiagajie',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.EventWhereUniqueInput,
        include: Optional[types.EventInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Event record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Event filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Event model

        Returns
        -------
        prisma.models.Event
            The found Event record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        event = Event.prisma().find_unique_or_raise(
            where={
                'id': 'eeejidbif',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EventWhereInput] = None,
        cursor: Optional[types.EventWhereUniqueInput] = None,
        include: Optional[types.EventInclude] = None,
        order: Optional[Union[types.EventOrderByInput, List[types.EventOrderByInput]]] = None,
        distinct: Optional[List[types.EventScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Event records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Event records returned
        skip
            Ignore the first N results
        where
            Event filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Event model
        order
            Order the returned Event records by any field
        distinct
            Filter Event records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Event]
            The list of all Event records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Event records
        events = Event.prisma().find_many(take=10)

        # find the first 5 Event records ordered by the title field
        events = Event.prisma().find_many(
            take=5,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EventWhereInput] = None,
        cursor: Optional[types.EventWhereUniqueInput] = None,
        include: Optional[types.EventInclude] = None,
        order: Optional[Union[types.EventOrderByInput, List[types.EventOrderByInput]]] = None,
        distinct: Optional[List[types.EventScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Event record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Event filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Event model
        order
            Order the returned Event records by any field
        distinct
            Filter Event records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Event
            The first Event record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Event record ordered by the slug field
        event = Event.prisma().find_first(
            skip=1,
            order={
                'slug': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EventWhereInput] = None,
        cursor: Optional[types.EventWhereUniqueInput] = None,
        include: Optional[types.EventInclude] = None,
        order: Optional[Union[types.EventOrderByInput, List[types.EventOrderByInput]]] = None,
        distinct: Optional[List[types.EventScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Event record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Event filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Event model
        order
            Order the returned Event records by any field
        distinct
            Filter Event records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Event
            The first Event record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Event record ordered by the description field
        event = Event.prisma().find_first_or_raise(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.EventUpdateInput,
        where: types.EventWhereUniqueInput,
        include: Optional[types.EventInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Event record.

        Parameters
        ----------
        data
            Event record data specifying what to update
        where
            Event filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Event model

        Returns
        -------
        prisma.models.Event
            The updated Event record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        event = Event.prisma().update(
            where={
                'id': 'efgbahec',
            },
            data={
                # data to update the Event record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.EventWhereUniqueInput,
        data: types.EventUpsertInput,
        include: Optional[types.EventInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Event filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Event model

        Returns
        -------
        prisma.models.Event
            The created or updated Event record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        event = Event.prisma().upsert(
            where={
                'id': 'hgjaiebfb',
            },
            data={
                'create': {
                    'id': 'hgjaiebfb',
                    'organizationId': 'bdidcfdfjd',
                    'title': 'dfeggejja',
                    'slug': 'gehbgghbj',
                    'description': 'dfhaijeie',
                    'startDate': datetime.datetime.utcnow(),
                    'endDate': datetime.datetime.utcnow(),
                    'countryId': 'gbcdjgicb',
                    'stateId': 'biaibdagac',
                },
                'update': {
                    'organizationId': 'bdidcfdfjd',
                    'title': 'dfeggejja',
                    'slug': 'gehbgghbj',
                    'description': 'dfhaijeie',
                    'startDate': datetime.datetime.utcnow(),
                    'endDate': datetime.datetime.utcnow(),
                    'countryId': 'gbcdjgicb',
                    'stateId': 'biaibdagac',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.EventUpdateManyMutationInput,
        where: types.EventWhereInput,
    ) -> int:
        """Update multiple Event records

        Parameters
        ----------
        data
            Event data to update the selected Event records to
        where
            Filter to select the Event records to update

        Returns
        -------
        int
            The total number of Event records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Event records
        total = Event.prisma().update_many(
            data={
                'startDate': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EventWhereInput] = None,
        cursor: Optional[types.EventWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Event records present in the database

        Parameters
        ----------
        select
            Select the Event fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Event filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EventCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Event.prisma().count()

        # results: prisma.types.EventCountAggregateOutput
        results = Event.prisma().count(
            select={
                '_all': True,
                'endDate': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.EventCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EventWhereInput] = None,
        cursor: Optional[types.EventWhereUniqueInput] = None,
    ) -> types.EventCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.EventCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EventWhereInput] = None,
        cursor: Optional[types.EventWhereUniqueInput] = None,
    ) -> Union[int, types.EventCountAggregateOutput]:
        """Count the number of Event records present in the database

        Parameters
        ----------
        select
            Select the Event fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Event filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EventCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Event.prisma().count()

        # results: prisma.types.EventCountAggregateOutput
        results = Event.prisma().count(
            select={
                '_all': True,
                'countryId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.EventCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.EventWhereInput] = None
    ) -> int:
        """Delete multiple Event records.

        Parameters
        ----------
        where
            Optional Event filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Event records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Event records
        total = Event.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.EventScalarFieldKeys'],
        *,
        where: Optional['types.EventWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.EventAvgAggregateInput'] = None,
        sum: Optional['types.EventSumAggregateInput'] = None,
        min: Optional['types.EventMinAggregateInput'] = None,
        max: Optional['types.EventMaxAggregateInput'] = None,
        having: Optional['types.EventScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.EventCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.EventScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.EventScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.EventGroupByOutput']:
        """Group Event records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Event fields to group records by
        where
            Event filter to select records
        take
            Limit the maximum number of Event records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.EventGroupByOutput]
            A list of dictionaries representing the Event record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Event records by stateId values
        # and count how many records are in each group
        results = Event.prisma().group_by(
            ['stateId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReservationsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Reservations]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Reservations.prisma().query_raw(
            'SELECT * FROM Reservations WHERE id = $1',
            'bddefjjabc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Reservations
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Reservations.prisma().query_first(
            'SELECT * FROM Reservations WHERE userId = $1',
            'bbbghgbadh',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ReservationsCreateInput,
        include: Optional[types.ReservationsInclude] = None
    ) -> _PrismaModelT:
        """Create a new Reservations record.

        Parameters
        ----------
        data
            Reservations record data
        include
            Specifies which relations should be loaded on the returned Reservations model

        Returns
        -------
        prisma.models.Reservations
            The created Reservations record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Reservations record from just the required fields
        reservations = Reservations.prisma().create(
            data={
                # data to create a Reservations record
                'userId': 'bbhcgagaic',
                'eventId': 'ddaabegbb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ReservationsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Reservations records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Reservations record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Reservations.prisma().create_many(
            data=[
                {
                    # data to create a Reservations record
                    'userId': 'bhgibfgbbc',
                    'eventId': 'hbgcihef',
                },
                {
                    # data to create a Reservations record
                    'userId': 'ffhgghde',
                    'eventId': 'ibcadcejf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ReservationsWhereUniqueInput,
        include: Optional[types.ReservationsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Reservations record.

        Parameters
        ----------
        where
            Reservations filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Reservations model

        Returns
        -------
        prisma.models.Reservations
            The deleted Reservations record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservations = Reservations.prisma().delete(
            where={
                'id': 'bdcdfgccdg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ReservationsWhereUniqueInput,
        include: Optional[types.ReservationsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Reservations record.

        Parameters
        ----------
        where
            Reservations filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Reservations model

        Returns
        -------
        prisma.models.Reservations
            The found Reservations record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservations = Reservations.prisma().find_unique(
            where={
                'id': 'edhjgdfh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ReservationsWhereUniqueInput,
        include: Optional[types.ReservationsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Reservations record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Reservations filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Reservations model

        Returns
        -------
        prisma.models.Reservations
            The found Reservations record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservations = Reservations.prisma().find_unique_or_raise(
            where={
                'id': 'bdeffdadda',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsWhereInput] = None,
        cursor: Optional[types.ReservationsWhereUniqueInput] = None,
        include: Optional[types.ReservationsInclude] = None,
        order: Optional[Union[types.ReservationsOrderByInput, List[types.ReservationsOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Reservations records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Reservations records returned
        skip
            Ignore the first N results
        where
            Reservations filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Reservations model
        order
            Order the returned Reservations records by any field
        distinct
            Filter Reservations records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Reservations]
            The list of all Reservations records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Reservations records
        reservations = Reservations.prisma().find_many(take=10)

        # find the first 5 Reservations records ordered by the termsAccepted field
        reservations = Reservations.prisma().find_many(
            take=5,
            order={
                'termsAccepted': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsWhereInput] = None,
        cursor: Optional[types.ReservationsWhereUniqueInput] = None,
        include: Optional[types.ReservationsInclude] = None,
        order: Optional[Union[types.ReservationsOrderByInput, List[types.ReservationsOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Reservations record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Reservations filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Reservations model
        order
            Order the returned Reservations records by any field
        distinct
            Filter Reservations records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Reservations
            The first Reservations record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Reservations record ordered by the imageRightsAccepted field
        reservations = Reservations.prisma().find_first(
            skip=1,
            order={
                'imageRightsAccepted': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsWhereInput] = None,
        cursor: Optional[types.ReservationsWhereUniqueInput] = None,
        include: Optional[types.ReservationsInclude] = None,
        order: Optional[Union[types.ReservationsOrderByInput, List[types.ReservationsOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Reservations record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Reservations filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Reservations model
        order
            Order the returned Reservations records by any field
        distinct
            Filter Reservations records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Reservations
            The first Reservations record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Reservations record ordered by the eventId field
        reservations = Reservations.prisma().find_first_or_raise(
            skip=1,
            order={
                'eventId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ReservationsUpdateInput,
        where: types.ReservationsWhereUniqueInput,
        include: Optional[types.ReservationsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Reservations record.

        Parameters
        ----------
        data
            Reservations record data specifying what to update
        where
            Reservations filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Reservations model

        Returns
        -------
        prisma.models.Reservations
            The updated Reservations record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        reservations = Reservations.prisma().update(
            where={
                'id': 'bjgfdihchf',
            },
            data={
                # data to update the Reservations record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ReservationsWhereUniqueInput,
        data: types.ReservationsUpsertInput,
        include: Optional[types.ReservationsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Reservations filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Reservations model

        Returns
        -------
        prisma.models.Reservations
            The created or updated Reservations record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservations = Reservations.prisma().upsert(
            where={
                'id': 'iaeihdeei',
            },
            data={
                'create': {
                    'id': 'iaeihdeei',
                    'userId': 'ffhgghde',
                    'eventId': 'ibcadcejf',
                },
                'update': {
                    'userId': 'ffhgghde',
                    'eventId': 'ibcadcejf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ReservationsUpdateManyMutationInput,
        where: types.ReservationsWhereInput,
    ) -> int:
        """Update multiple Reservations records

        Parameters
        ----------
        data
            Reservations data to update the selected Reservations records to
        where
            Filter to select the Reservations records to update

        Returns
        -------
        int
            The total number of Reservations records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Reservations records
        total = Reservations.prisma().update_many(
            data={
                'reservationDate': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsWhereInput] = None,
        cursor: Optional[types.ReservationsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Reservations records present in the database

        Parameters
        ----------
        select
            Select the Reservations fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Reservations filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReservationsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Reservations.prisma().count()

        # results: prisma.types.ReservationsCountAggregateOutput
        results = Reservations.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ReservationsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsWhereInput] = None,
        cursor: Optional[types.ReservationsWhereUniqueInput] = None,
    ) -> types.ReservationsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ReservationsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsWhereInput] = None,
        cursor: Optional[types.ReservationsWhereUniqueInput] = None,
    ) -> Union[int, types.ReservationsCountAggregateOutput]:
        """Count the number of Reservations records present in the database

        Parameters
        ----------
        select
            Select the Reservations fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Reservations filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReservationsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Reservations.prisma().count()

        # results: prisma.types.ReservationsCountAggregateOutput
        results = Reservations.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReservationsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ReservationsWhereInput] = None
    ) -> int:
        """Delete multiple Reservations records.

        Parameters
        ----------
        where
            Optional Reservations filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Reservations records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Reservations records
        total = Reservations.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ReservationsScalarFieldKeys'],
        *,
        where: Optional['types.ReservationsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReservationsAvgAggregateInput'] = None,
        sum: Optional['types.ReservationsSumAggregateInput'] = None,
        min: Optional['types.ReservationsMinAggregateInput'] = None,
        max: Optional['types.ReservationsMaxAggregateInput'] = None,
        having: Optional['types.ReservationsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReservationsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReservationsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReservationsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReservationsGroupByOutput']:
        """Group Reservations records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Reservations fields to group records by
        where
            Reservations filter to select records
        take
            Limit the maximum number of Reservations records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReservationsGroupByOutput]
            A list of dictionaries representing the Reservations record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Reservations records by pastoralLetterUploaded values
        # and count how many records are in each group
        results = Reservations.prisma().group_by(
            ['pastoralLetterUploaded'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FilesActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Files]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Files.prisma().query_raw(
            'SELECT * FROM Files WHERE id = $1',
            'bfggejgfbd',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Files
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Files.prisma().query_first(
            'SELECT * FROM Files WHERE name = $1',
            'ifaaaedja',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FilesCreateInput,
        include: Optional[types.FilesInclude] = None
    ) -> _PrismaModelT:
        """Create a new Files record.

        Parameters
        ----------
        data
            Files record data
        include
            Specifies which relations should be loaded on the returned Files model

        Returns
        -------
        prisma.models.Files
            The created Files record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Files record from just the required fields
        files = Files.prisma().create(
            data={
                # data to create a Files record
                'name': 'cbajdjjabf',
                'mimeType': 'bcicggedea',
                'driveFileId': 'cebcdadjh',
                'url': 'ehfigdgac',
                'uploadedById': 'bhbgccijjf',
                'fileableId': 'bigjhdgbjc',
                'fileableType': 'bfifdebhfd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FilesCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Files records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Files record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Files.prisma().create_many(
            data=[
                {
                    # data to create a Files record
                    'name': 'cjchbjde',
                    'mimeType': 'bfiibjcehj',
                    'driveFileId': 'ijieafghg',
                    'url': 'hhhegahcf',
                    'uploadedById': 'edhijefdi',
                    'fileableId': 'djddecjhb',
                    'fileableType': 'bgdicjhie',
                },
                {
                    # data to create a Files record
                    'name': 'ceibfcgij',
                    'mimeType': 'debhbfada',
                    'driveFileId': 'bgjchggecd',
                    'url': 'igggcfjg',
                    'uploadedById': 'bgjhijffjh',
                    'fileableId': 'bcigdhache',
                    'fileableType': 'igefhgdhb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FilesWhereUniqueInput,
        include: Optional[types.FilesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Files record.

        Parameters
        ----------
        where
            Files filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Files model

        Returns
        -------
        prisma.models.Files
            The deleted Files record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        files = Files.prisma().delete(
            where={
                'id': 'ejbiifbae',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FilesWhereUniqueInput,
        include: Optional[types.FilesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Files record.

        Parameters
        ----------
        where
            Files filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Files model

        Returns
        -------
        prisma.models.Files
            The found Files record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        files = Files.prisma().find_unique(
            where={
                'id': 'djcfgedjd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FilesWhereUniqueInput,
        include: Optional[types.FilesInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Files record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Files filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Files model

        Returns
        -------
        prisma.models.Files
            The found Files record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        files = Files.prisma().find_unique_or_raise(
            where={
                'id': 'bdbjcdegag',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FilesWhereInput] = None,
        cursor: Optional[types.FilesWhereUniqueInput] = None,
        include: Optional[types.FilesInclude] = None,
        order: Optional[Union[types.FilesOrderByInput, List[types.FilesOrderByInput]]] = None,
        distinct: Optional[List[types.FilesScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Files records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Files records returned
        skip
            Ignore the first N results
        where
            Files filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Files model
        order
            Order the returned Files records by any field
        distinct
            Filter Files records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Files]
            The list of all Files records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Files records
        files = Files.prisma().find_many(take=10)

        # find the first 5 Files records ordered by the mimeType field
        files = Files.prisma().find_many(
            take=5,
            order={
                'mimeType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FilesWhereInput] = None,
        cursor: Optional[types.FilesWhereUniqueInput] = None,
        include: Optional[types.FilesInclude] = None,
        order: Optional[Union[types.FilesOrderByInput, List[types.FilesOrderByInput]]] = None,
        distinct: Optional[List[types.FilesScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Files record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Files filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Files model
        order
            Order the returned Files records by any field
        distinct
            Filter Files records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Files
            The first Files record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Files record ordered by the driveFileId field
        files = Files.prisma().find_first(
            skip=1,
            order={
                'driveFileId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FilesWhereInput] = None,
        cursor: Optional[types.FilesWhereUniqueInput] = None,
        include: Optional[types.FilesInclude] = None,
        order: Optional[Union[types.FilesOrderByInput, List[types.FilesOrderByInput]]] = None,
        distinct: Optional[List[types.FilesScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Files record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Files filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Files model
        order
            Order the returned Files records by any field
        distinct
            Filter Files records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Files
            The first Files record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Files record ordered by the url field
        files = Files.prisma().find_first_or_raise(
            skip=1,
            order={
                'url': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FilesUpdateInput,
        where: types.FilesWhereUniqueInput,
        include: Optional[types.FilesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Files record.

        Parameters
        ----------
        data
            Files record data specifying what to update
        where
            Files filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Files model

        Returns
        -------
        prisma.models.Files
            The updated Files record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        files = Files.prisma().update(
            where={
                'id': 'hbchfebch',
            },
            data={
                # data to update the Files record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FilesWhereUniqueInput,
        data: types.FilesUpsertInput,
        include: Optional[types.FilesInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Files filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Files model

        Returns
        -------
        prisma.models.Files
            The created or updated Files record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        files = Files.prisma().upsert(
            where={
                'id': 'bcjjffegfc',
            },
            data={
                'create': {
                    'id': 'bcjjffegfc',
                    'name': 'ceibfcgij',
                    'mimeType': 'debhbfada',
                    'driveFileId': 'bgjchggecd',
                    'url': 'igggcfjg',
                    'uploadedById': 'bgjhijffjh',
                    'fileableId': 'bcigdhache',
                    'fileableType': 'igefhgdhb',
                },
                'update': {
                    'name': 'ceibfcgij',
                    'mimeType': 'debhbfada',
                    'driveFileId': 'bgjchggecd',
                    'url': 'igggcfjg',
                    'uploadedById': 'bgjhijffjh',
                    'fileableId': 'bcigdhache',
                    'fileableType': 'igefhgdhb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FilesUpdateManyMutationInput,
        where: types.FilesWhereInput,
    ) -> int:
        """Update multiple Files records

        Parameters
        ----------
        data
            Files data to update the selected Files records to
        where
            Filter to select the Files records to update

        Returns
        -------
        int
            The total number of Files records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Files records
        total = Files.prisma().update_many(
            data={
                'uploadedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FilesWhereInput] = None,
        cursor: Optional[types.FilesWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Files records present in the database

        Parameters
        ----------
        select
            Select the Files fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Files filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FilesCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Files.prisma().count()

        # results: prisma.types.FilesCountAggregateOutput
        results = Files.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FilesCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FilesWhereInput] = None,
        cursor: Optional[types.FilesWhereUniqueInput] = None,
    ) -> types.FilesCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FilesCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FilesWhereInput] = None,
        cursor: Optional[types.FilesWhereUniqueInput] = None,
    ) -> Union[int, types.FilesCountAggregateOutput]:
        """Count the number of Files records present in the database

        Parameters
        ----------
        select
            Select the Files fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Files filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FilesCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Files.prisma().count()

        # results: prisma.types.FilesCountAggregateOutput
        results = Files.prisma().count(
            select={
                '_all': True,
                'uploadedById': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FilesCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FilesWhereInput] = None
    ) -> int:
        """Delete multiple Files records.

        Parameters
        ----------
        where
            Optional Files filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Files records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Files records
        total = Files.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FilesScalarFieldKeys'],
        *,
        where: Optional['types.FilesWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FilesAvgAggregateInput'] = None,
        sum: Optional['types.FilesSumAggregateInput'] = None,
        min: Optional['types.FilesMinAggregateInput'] = None,
        max: Optional['types.FilesMaxAggregateInput'] = None,
        having: Optional['types.FilesScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FilesCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FilesScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FilesScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FilesGroupByOutput']:
        """Group Files records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Files fields to group records by
        where
            Files filter to select records
        take
            Limit the maximum number of Files records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FilesGroupByOutput]
            A list of dictionaries representing the Files record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Files records by fileableId values
        # and count how many records are in each group
        results = Files.prisma().group_by(
            ['fileableId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PaymentActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Payment]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Payment.prisma().query_raw(
            'SELECT * FROM Payment WHERE id = $1',
            'cahaeaicjd',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Payment
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Payment.prisma().query_first(
            'SELECT * FROM Payment WHERE userId = $1',
            'ibbjaacbi',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PaymentCreateInput,
        include: Optional[types.PaymentInclude] = None
    ) -> _PrismaModelT:
        """Create a new Payment record.

        Parameters
        ----------
        data
            Payment record data
        include
            Specifies which relations should be loaded on the returned Payment model

        Returns
        -------
        prisma.models.Payment
            The created Payment record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Payment record from just the required fields
        payment = Payment.prisma().create(
            data={
                # data to create a Payment record
                'userId': 'djgacbcch',
                'amount': 644446024.11638,
                'currency': 'dajcifgdi',
                'paymentMethod': 'ccedhdbj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PaymentCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Payment records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Payment record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Payment.prisma().create_many(
            data=[
                {
                    # data to create a Payment record
                    'userId': 'bjaabjjjce',
                    'amount': 2057323293.134411,
                    'currency': 'cafcbdchah',
                    'paymentMethod': 'bdffbehbae',
                },
                {
                    # data to create a Payment record
                    'userId': 'ieahjgeb',
                    'amount': 754433246.31426,
                    'currency': 'cchghigae',
                    'paymentMethod': 'ecdjjjhab',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PaymentWhereUniqueInput,
        include: Optional[types.PaymentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Payment record.

        Parameters
        ----------
        where
            Payment filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Payment model

        Returns
        -------
        prisma.models.Payment
            The deleted Payment record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        payment = Payment.prisma().delete(
            where={
                'id': 'biachfede',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PaymentWhereUniqueInput,
        include: Optional[types.PaymentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Payment record.

        Parameters
        ----------
        where
            Payment filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Payment model

        Returns
        -------
        prisma.models.Payment
            The found Payment record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        payment = Payment.prisma().find_unique(
            where={
                'id': 'fhgaibff',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PaymentWhereUniqueInput,
        include: Optional[types.PaymentInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Payment record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Payment filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Payment model

        Returns
        -------
        prisma.models.Payment
            The found Payment record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        payment = Payment.prisma().find_unique_or_raise(
            where={
                'id': 'cadajbcbca',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaymentWhereInput] = None,
        cursor: Optional[types.PaymentWhereUniqueInput] = None,
        include: Optional[types.PaymentInclude] = None,
        order: Optional[Union[types.PaymentOrderByInput, List[types.PaymentOrderByInput]]] = None,
        distinct: Optional[List[types.PaymentScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Payment records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Payment records returned
        skip
            Ignore the first N results
        where
            Payment filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Payment model
        order
            Order the returned Payment records by any field
        distinct
            Filter Payment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Payment]
            The list of all Payment records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Payment records
        payments = Payment.prisma().find_many(take=10)

        # find the first 5 Payment records ordered by the amount field
        payments = Payment.prisma().find_many(
            take=5,
            order={
                'amount': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PaymentWhereInput] = None,
        cursor: Optional[types.PaymentWhereUniqueInput] = None,
        include: Optional[types.PaymentInclude] = None,
        order: Optional[Union[types.PaymentOrderByInput, List[types.PaymentOrderByInput]]] = None,
        distinct: Optional[List[types.PaymentScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Payment record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Payment filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Payment model
        order
            Order the returned Payment records by any field
        distinct
            Filter Payment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Payment
            The first Payment record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Payment record ordered by the currency field
        payment = Payment.prisma().find_first(
            skip=1,
            order={
                'currency': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PaymentWhereInput] = None,
        cursor: Optional[types.PaymentWhereUniqueInput] = None,
        include: Optional[types.PaymentInclude] = None,
        order: Optional[Union[types.PaymentOrderByInput, List[types.PaymentOrderByInput]]] = None,
        distinct: Optional[List[types.PaymentScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Payment record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Payment filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Payment model
        order
            Order the returned Payment records by any field
        distinct
            Filter Payment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Payment
            The first Payment record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Payment record ordered by the paymentMethod field
        payment = Payment.prisma().find_first_or_raise(
            skip=1,
            order={
                'paymentMethod': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PaymentUpdateInput,
        where: types.PaymentWhereUniqueInput,
        include: Optional[types.PaymentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Payment record.

        Parameters
        ----------
        data
            Payment record data specifying what to update
        where
            Payment filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Payment model

        Returns
        -------
        prisma.models.Payment
            The updated Payment record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        payment = Payment.prisma().update(
            where={
                'id': 'bjheigfcdd',
            },
            data={
                # data to update the Payment record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PaymentWhereUniqueInput,
        data: types.PaymentUpsertInput,
        include: Optional[types.PaymentInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Payment filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Payment model

        Returns
        -------
        prisma.models.Payment
            The created or updated Payment record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        payment = Payment.prisma().upsert(
            where={
                'id': 'bjejigcdcg',
            },
            data={
                'create': {
                    'id': 'bjejigcdcg',
                    'userId': 'ieahjgeb',
                    'amount': 754433246.31426,
                    'currency': 'cchghigae',
                    'paymentMethod': 'ecdjjjhab',
                },
                'update': {
                    'userId': 'ieahjgeb',
                    'amount': 754433246.31426,
                    'currency': 'cchghigae',
                    'paymentMethod': 'ecdjjjhab',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PaymentUpdateManyMutationInput,
        where: types.PaymentWhereInput,
    ) -> int:
        """Update multiple Payment records

        Parameters
        ----------
        data
            Payment data to update the selected Payment records to
        where
            Filter to select the Payment records to update

        Returns
        -------
        int
            The total number of Payment records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Payment records
        total = Payment.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaymentWhereInput] = None,
        cursor: Optional[types.PaymentWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Payment records present in the database

        Parameters
        ----------
        select
            Select the Payment fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Payment filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PaymentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Payment.prisma().count()

        # results: prisma.types.PaymentCountAggregateOutput
        results = Payment.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PaymentCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaymentWhereInput] = None,
        cursor: Optional[types.PaymentWhereUniqueInput] = None,
    ) -> types.PaymentCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PaymentCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaymentWhereInput] = None,
        cursor: Optional[types.PaymentWhereUniqueInput] = None,
    ) -> Union[int, types.PaymentCountAggregateOutput]:
        """Count the number of Payment records present in the database

        Parameters
        ----------
        select
            Select the Payment fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Payment filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PaymentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Payment.prisma().count()

        # results: prisma.types.PaymentCountAggregateOutput
        results = Payment.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PaymentCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PaymentWhereInput] = None
    ) -> int:
        """Delete multiple Payment records.

        Parameters
        ----------
        where
            Optional Payment filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Payment records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Payment records
        total = Payment.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PaymentScalarFieldKeys'],
        *,
        where: Optional['types.PaymentWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PaymentAvgAggregateInput'] = None,
        sum: Optional['types.PaymentSumAggregateInput'] = None,
        min: Optional['types.PaymentMinAggregateInput'] = None,
        max: Optional['types.PaymentMaxAggregateInput'] = None,
        having: Optional['types.PaymentScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PaymentCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PaymentScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PaymentScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PaymentGroupByOutput']:
        """Group Payment records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Payment fields to group records by
        where
            Payment filter to select records
        take
            Limit the maximum number of Payment records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PaymentGroupByOutput]
            A list of dictionaries representing the Payment record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Payment records by id values
        # and count how many records are in each group
        results = Payment.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReservationsPaymentsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ReservationsPayments]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ReservationsPayments.prisma().query_raw(
            'SELECT * FROM ReservationsPayments WHERE id = $1',
            'bifiiibcah',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ReservationsPayments
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ReservationsPayments.prisma().query_first(
            'SELECT * FROM ReservationsPayments WHERE reservationId = $1',
            'dbjibjdaa',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ReservationsPaymentsCreateInput,
        include: Optional[types.ReservationsPaymentsInclude] = None
    ) -> _PrismaModelT:
        """Create a new ReservationsPayments record.

        Parameters
        ----------
        data
            ReservationsPayments record data
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model

        Returns
        -------
        prisma.models.ReservationsPayments
            The created ReservationsPayments record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ReservationsPayments record from just the required fields
        reservationspayments = ReservationsPayments.prisma().create(
            data={
                # data to create a ReservationsPayments record
                'reservationId': 'dgijbdiaf',
                'paymentId': 'begfaigba',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ReservationsPaymentsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ReservationsPayments records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ReservationsPayments record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ReservationsPayments.prisma().create_many(
            data=[
                {
                    # data to create a ReservationsPayments record
                    'reservationId': 'bdjiafcgjb',
                    'paymentId': 'bficecgcfg',
                },
                {
                    # data to create a ReservationsPayments record
                    'reservationId': 'cbjjeedcj',
                    'paymentId': 'dedgbbhja',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ReservationsPaymentsWhereUniqueInput,
        include: Optional[types.ReservationsPaymentsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ReservationsPayments record.

        Parameters
        ----------
        where
            ReservationsPayments filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model

        Returns
        -------
        prisma.models.ReservationsPayments
            The deleted ReservationsPayments record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservationspayments = ReservationsPayments.prisma().delete(
            where={
                'id': 'cabiahchj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ReservationsPaymentsWhereUniqueInput,
        include: Optional[types.ReservationsPaymentsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ReservationsPayments record.

        Parameters
        ----------
        where
            ReservationsPayments filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model

        Returns
        -------
        prisma.models.ReservationsPayments
            The found ReservationsPayments record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservationspayments = ReservationsPayments.prisma().find_unique(
            where={
                'id': 'cgbeccfce',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ReservationsPaymentsWhereUniqueInput,
        include: Optional[types.ReservationsPaymentsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ReservationsPayments record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ReservationsPayments filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model

        Returns
        -------
        prisma.models.ReservationsPayments
            The found ReservationsPayments record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservationspayments = ReservationsPayments.prisma().find_unique_or_raise(
            where={
                'id': 'fcjcagef',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsPaymentsWhereInput] = None,
        cursor: Optional[types.ReservationsPaymentsWhereUniqueInput] = None,
        include: Optional[types.ReservationsPaymentsInclude] = None,
        order: Optional[Union[types.ReservationsPaymentsOrderByInput, List[types.ReservationsPaymentsOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationsPaymentsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ReservationsPayments records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ReservationsPayments records returned
        skip
            Ignore the first N results
        where
            ReservationsPayments filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model
        order
            Order the returned ReservationsPayments records by any field
        distinct
            Filter ReservationsPayments records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ReservationsPayments]
            The list of all ReservationsPayments records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ReservationsPayments records
        reservationspayments = ReservationsPayments.prisma().find_many(take=10)

        # find the first 5 ReservationsPayments records ordered by the paymentId field
        reservationspayments = ReservationsPayments.prisma().find_many(
            take=5,
            order={
                'paymentId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsPaymentsWhereInput] = None,
        cursor: Optional[types.ReservationsPaymentsWhereUniqueInput] = None,
        include: Optional[types.ReservationsPaymentsInclude] = None,
        order: Optional[Union[types.ReservationsPaymentsOrderByInput, List[types.ReservationsPaymentsOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationsPaymentsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ReservationsPayments record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReservationsPayments filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model
        order
            Order the returned ReservationsPayments records by any field
        distinct
            Filter ReservationsPayments records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReservationsPayments
            The first ReservationsPayments record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReservationsPayments record ordered by the createdAt field
        reservationspayments = ReservationsPayments.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsPaymentsWhereInput] = None,
        cursor: Optional[types.ReservationsPaymentsWhereUniqueInput] = None,
        include: Optional[types.ReservationsPaymentsInclude] = None,
        order: Optional[Union[types.ReservationsPaymentsOrderByInput, List[types.ReservationsPaymentsOrderByInput]]] = None,
        distinct: Optional[List[types.ReservationsPaymentsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ReservationsPayments record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReservationsPayments filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model
        order
            Order the returned ReservationsPayments records by any field
        distinct
            Filter ReservationsPayments records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReservationsPayments
            The first ReservationsPayments record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReservationsPayments record ordered by the updatedAt field
        reservationspayments = ReservationsPayments.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ReservationsPaymentsUpdateInput,
        where: types.ReservationsPaymentsWhereUniqueInput,
        include: Optional[types.ReservationsPaymentsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ReservationsPayments record.

        Parameters
        ----------
        data
            ReservationsPayments record data specifying what to update
        where
            ReservationsPayments filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model

        Returns
        -------
        prisma.models.ReservationsPayments
            The updated ReservationsPayments record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        reservationspayments = ReservationsPayments.prisma().update(
            where={
                'id': 'bgdhaeacic',
            },
            data={
                # data to update the ReservationsPayments record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ReservationsPaymentsWhereUniqueInput,
        data: types.ReservationsPaymentsUpsertInput,
        include: Optional[types.ReservationsPaymentsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ReservationsPayments filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ReservationsPayments model

        Returns
        -------
        prisma.models.ReservationsPayments
            The created or updated ReservationsPayments record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        reservationspayments = ReservationsPayments.prisma().upsert(
            where={
                'id': 'caffafcheh',
            },
            data={
                'create': {
                    'id': 'caffafcheh',
                    'reservationId': 'cbjjeedcj',
                    'paymentId': 'dedgbbhja',
                },
                'update': {
                    'reservationId': 'cbjjeedcj',
                    'paymentId': 'dedgbbhja',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ReservationsPaymentsUpdateManyMutationInput,
        where: types.ReservationsPaymentsWhereInput,
    ) -> int:
        """Update multiple ReservationsPayments records

        Parameters
        ----------
        data
            ReservationsPayments data to update the selected ReservationsPayments records to
        where
            Filter to select the ReservationsPayments records to update

        Returns
        -------
        int
            The total number of ReservationsPayments records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ReservationsPayments records
        total = ReservationsPayments.prisma().update_many(
            data={
                'id': 'fjjbegge'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsPaymentsWhereInput] = None,
        cursor: Optional[types.ReservationsPaymentsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ReservationsPayments records present in the database

        Parameters
        ----------
        select
            Select the ReservationsPayments fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReservationsPayments filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReservationsPaymentsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ReservationsPayments.prisma().count()

        # results: prisma.types.ReservationsPaymentsCountAggregateOutput
        results = ReservationsPayments.prisma().count(
            select={
                '_all': True,
                'reservationId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ReservationsPaymentsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsPaymentsWhereInput] = None,
        cursor: Optional[types.ReservationsPaymentsWhereUniqueInput] = None,
    ) -> types.ReservationsPaymentsCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ReservationsPaymentsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReservationsPaymentsWhereInput] = None,
        cursor: Optional[types.ReservationsPaymentsWhereUniqueInput] = None,
    ) -> Union[int, types.ReservationsPaymentsCountAggregateOutput]:
        """Count the number of ReservationsPayments records present in the database

        Parameters
        ----------
        select
            Select the ReservationsPayments fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReservationsPayments filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReservationsPaymentsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ReservationsPayments.prisma().count()

        # results: prisma.types.ReservationsPaymentsCountAggregateOutput
        results = ReservationsPayments.prisma().count(
            select={
                '_all': True,
                'paymentId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReservationsPaymentsCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ReservationsPaymentsWhereInput] = None
    ) -> int:
        """Delete multiple ReservationsPayments records.

        Parameters
        ----------
        where
            Optional ReservationsPayments filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ReservationsPayments records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ReservationsPayments records
        total = ReservationsPayments.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ReservationsPaymentsScalarFieldKeys'],
        *,
        where: Optional['types.ReservationsPaymentsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReservationsPaymentsAvgAggregateInput'] = None,
        sum: Optional['types.ReservationsPaymentsSumAggregateInput'] = None,
        min: Optional['types.ReservationsPaymentsMinAggregateInput'] = None,
        max: Optional['types.ReservationsPaymentsMaxAggregateInput'] = None,
        having: Optional['types.ReservationsPaymentsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReservationsPaymentsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReservationsPaymentsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReservationsPaymentsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReservationsPaymentsGroupByOutput']:
        """Group ReservationsPayments records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ReservationsPayments fields to group records by
        where
            ReservationsPayments filter to select records
        take
            Limit the maximum number of ReservationsPayments records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReservationsPaymentsGroupByOutput]
            A list of dictionaries representing the ReservationsPayments record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ReservationsPayments records by createdAt values
        # and count how many records are in each group
        results = ReservationsPayments.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FinancialMovementActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FinancialMovement]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FinancialMovement.prisma().query_raw(
            'SELECT * FROM FinancialMovement WHERE id = $1',
            'bdiifhbieb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FinancialMovement
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FinancialMovement.prisma().query_first(
            'SELECT * FROM FinancialMovement WHERE eventId = $1',
            'cdcaejhgg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FinancialMovementCreateInput,
        include: Optional[types.FinancialMovementInclude] = None
    ) -> _PrismaModelT:
        """Create a new FinancialMovement record.

        Parameters
        ----------
        data
            FinancialMovement record data
        include
            Specifies which relations should be loaded on the returned FinancialMovement model

        Returns
        -------
        prisma.models.FinancialMovement
            The created FinancialMovement record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FinancialMovement record from just the required fields
        financialmovement = FinancialMovement.prisma().create(
            data={
                # data to create a FinancialMovement record
                'eventId': 'jbijgfbfj',
                'type': enums.FinancialMovementType.INCOME,
                'concept': 'ggfbeddia',
                'amount': 39949309.190115,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FinancialMovementCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FinancialMovement records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FinancialMovement record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FinancialMovement.prisma().create_many(
            data=[
                {
                    # data to create a FinancialMovement record
                    'eventId': 'bgchfbjibb',
                    'type': enums.FinancialMovementType.INCOME,
                    'concept': 'bajecchdjc',
                    'amount': 356020985.163833,
                },
                {
                    # data to create a FinancialMovement record
                    'eventId': 'bijbfghhhf',
                    'type': enums.FinancialMovementType.INCOME,
                    'concept': 'bahchhihdc',
                    'amount': 1879328181.157791,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FinancialMovementWhereUniqueInput,
        include: Optional[types.FinancialMovementInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FinancialMovement record.

        Parameters
        ----------
        where
            FinancialMovement filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FinancialMovement model

        Returns
        -------
        prisma.models.FinancialMovement
            The deleted FinancialMovement record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovement = FinancialMovement.prisma().delete(
            where={
                'id': 'faehcjfdb',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FinancialMovementWhereUniqueInput,
        include: Optional[types.FinancialMovementInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FinancialMovement record.

        Parameters
        ----------
        where
            FinancialMovement filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FinancialMovement model

        Returns
        -------
        prisma.models.FinancialMovement
            The found FinancialMovement record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovement = FinancialMovement.prisma().find_unique(
            where={
                'id': 'bbaiefbee',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FinancialMovementWhereUniqueInput,
        include: Optional[types.FinancialMovementInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FinancialMovement record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FinancialMovement filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FinancialMovement model

        Returns
        -------
        prisma.models.FinancialMovement
            The found FinancialMovement record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovement = FinancialMovement.prisma().find_unique_or_raise(
            where={
                'id': 'bdaacgjbaf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementWhereInput] = None,
        cursor: Optional[types.FinancialMovementWhereUniqueInput] = None,
        include: Optional[types.FinancialMovementInclude] = None,
        order: Optional[Union[types.FinancialMovementOrderByInput, List[types.FinancialMovementOrderByInput]]] = None,
        distinct: Optional[List[types.FinancialMovementScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FinancialMovement records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FinancialMovement records returned
        skip
            Ignore the first N results
        where
            FinancialMovement filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinancialMovement model
        order
            Order the returned FinancialMovement records by any field
        distinct
            Filter FinancialMovement records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FinancialMovement]
            The list of all FinancialMovement records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FinancialMovement records
        financialmovements = FinancialMovement.prisma().find_many(take=10)

        # find the first 5 FinancialMovement records ordered by the type field
        financialmovements = FinancialMovement.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementWhereInput] = None,
        cursor: Optional[types.FinancialMovementWhereUniqueInput] = None,
        include: Optional[types.FinancialMovementInclude] = None,
        order: Optional[Union[types.FinancialMovementOrderByInput, List[types.FinancialMovementOrderByInput]]] = None,
        distinct: Optional[List[types.FinancialMovementScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FinancialMovement record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FinancialMovement filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinancialMovement model
        order
            Order the returned FinancialMovement records by any field
        distinct
            Filter FinancialMovement records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FinancialMovement
            The first FinancialMovement record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FinancialMovement record ordered by the concept field
        financialmovement = FinancialMovement.prisma().find_first(
            skip=1,
            order={
                'concept': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementWhereInput] = None,
        cursor: Optional[types.FinancialMovementWhereUniqueInput] = None,
        include: Optional[types.FinancialMovementInclude] = None,
        order: Optional[Union[types.FinancialMovementOrderByInput, List[types.FinancialMovementOrderByInput]]] = None,
        distinct: Optional[List[types.FinancialMovementScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FinancialMovement record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FinancialMovement filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinancialMovement model
        order
            Order the returned FinancialMovement records by any field
        distinct
            Filter FinancialMovement records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FinancialMovement
            The first FinancialMovement record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FinancialMovement record ordered by the amount field
        financialmovement = FinancialMovement.prisma().find_first_or_raise(
            skip=1,
            order={
                'amount': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FinancialMovementUpdateInput,
        where: types.FinancialMovementWhereUniqueInput,
        include: Optional[types.FinancialMovementInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FinancialMovement record.

        Parameters
        ----------
        data
            FinancialMovement record data specifying what to update
        where
            FinancialMovement filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FinancialMovement model

        Returns
        -------
        prisma.models.FinancialMovement
            The updated FinancialMovement record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        financialmovement = FinancialMovement.prisma().update(
            where={
                'id': 'biibaighec',
            },
            data={
                # data to update the FinancialMovement record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FinancialMovementWhereUniqueInput,
        data: types.FinancialMovementUpsertInput,
        include: Optional[types.FinancialMovementInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FinancialMovement filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FinancialMovement model

        Returns
        -------
        prisma.models.FinancialMovement
            The created or updated FinancialMovement record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovement = FinancialMovement.prisma().upsert(
            where={
                'id': 'baicdfeidj',
            },
            data={
                'create': {
                    'id': 'baicdfeidj',
                    'eventId': 'bijbfghhhf',
                    'type': enums.FinancialMovementType.INCOME,
                    'concept': 'bahchhihdc',
                    'amount': 1879328181.157791,
                },
                'update': {
                    'eventId': 'bijbfghhhf',
                    'type': enums.FinancialMovementType.INCOME,
                    'concept': 'bahchhihdc',
                    'amount': 1879328181.157791,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FinancialMovementUpdateManyMutationInput,
        where: types.FinancialMovementWhereInput,
    ) -> int:
        """Update multiple FinancialMovement records

        Parameters
        ----------
        data
            FinancialMovement data to update the selected FinancialMovement records to
        where
            Filter to select the FinancialMovement records to update

        Returns
        -------
        int
            The total number of FinancialMovement records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FinancialMovement records
        total = FinancialMovement.prisma().update_many(
            data={
                'date': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementWhereInput] = None,
        cursor: Optional[types.FinancialMovementWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FinancialMovement records present in the database

        Parameters
        ----------
        select
            Select the FinancialMovement fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FinancialMovement filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FinancialMovementCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FinancialMovement.prisma().count()

        # results: prisma.types.FinancialMovementCountAggregateOutput
        results = FinancialMovement.prisma().count(
            select={
                '_all': True,
                'origin': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FinancialMovementCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementWhereInput] = None,
        cursor: Optional[types.FinancialMovementWhereUniqueInput] = None,
    ) -> types.FinancialMovementCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FinancialMovementCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementWhereInput] = None,
        cursor: Optional[types.FinancialMovementWhereUniqueInput] = None,
    ) -> Union[int, types.FinancialMovementCountAggregateOutput]:
        """Count the number of FinancialMovement records present in the database

        Parameters
        ----------
        select
            Select the FinancialMovement fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FinancialMovement filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FinancialMovementCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FinancialMovement.prisma().count()

        # results: prisma.types.FinancialMovementCountAggregateOutput
        results = FinancialMovement.prisma().count(
            select={
                '_all': True,
                'reservationId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FinancialMovementCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FinancialMovementWhereInput] = None
    ) -> int:
        """Delete multiple FinancialMovement records.

        Parameters
        ----------
        where
            Optional FinancialMovement filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FinancialMovement records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FinancialMovement records
        total = FinancialMovement.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FinancialMovementScalarFieldKeys'],
        *,
        where: Optional['types.FinancialMovementWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FinancialMovementAvgAggregateInput'] = None,
        sum: Optional['types.FinancialMovementSumAggregateInput'] = None,
        min: Optional['types.FinancialMovementMinAggregateInput'] = None,
        max: Optional['types.FinancialMovementMaxAggregateInput'] = None,
        having: Optional['types.FinancialMovementScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FinancialMovementCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FinancialMovementScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FinancialMovementScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FinancialMovementGroupByOutput']:
        """Group FinancialMovement records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FinancialMovement fields to group records by
        where
            FinancialMovement filter to select records
        take
            Limit the maximum number of FinancialMovement records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FinancialMovementGroupByOutput]
            A list of dictionaries representing the FinancialMovement record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FinancialMovement records by paymentId values
        # and count how many records are in each group
        results = FinancialMovement.prisma().group_by(
            ['paymentId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PurchaseOrderActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PurchaseOrder]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PurchaseOrder.prisma().query_raw(
            'SELECT * FROM PurchaseOrder WHERE id = $1',
            'befgiciadg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PurchaseOrder
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PurchaseOrder.prisma().query_first(
            'SELECT * FROM PurchaseOrder WHERE eventId = $1',
            'cbcehahedh',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PurchaseOrderCreateInput,
        include: Optional[types.PurchaseOrderInclude] = None
    ) -> _PrismaModelT:
        """Create a new PurchaseOrder record.

        Parameters
        ----------
        data
            PurchaseOrder record data
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model

        Returns
        -------
        prisma.models.PurchaseOrder
            The created PurchaseOrder record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PurchaseOrder record from just the required fields
        purchaseorder = PurchaseOrder.prisma().create(
            data={
                # data to create a PurchaseOrder record
                'eventId': 'bcjihiaide',
                'createdById': 'bagfijcgfj',
                'provider': 'bcggehiidc',
                'concept': 'bjcdacgacf',
                'totalAmount': 95844078.189591,
                'currency': 'cdcdjdcee',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PurchaseOrderCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PurchaseOrder records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PurchaseOrder record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PurchaseOrder.prisma().create_many(
            data=[
                {
                    # data to create a PurchaseOrder record
                    'eventId': 'bbbgjdbgcb',
                    'createdById': 'bcedacgecg',
                    'provider': 'cbdffjeh',
                    'concept': 'idbcdhbci',
                    'totalAmount': 1024647073.41434,
                    'currency': 'bajagjdfbb',
                },
                {
                    # data to create a PurchaseOrder record
                    'eventId': 'bggedbjggi',
                    'createdById': 'hgbafifcf',
                    'provider': 'bejiecfecg',
                    'concept': 'bjgacaeagh',
                    'totalAmount': 1440871456.151564,
                    'currency': 'jbgheibja',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PurchaseOrderWhereUniqueInput,
        include: Optional[types.PurchaseOrderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PurchaseOrder record.

        Parameters
        ----------
        where
            PurchaseOrder filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model

        Returns
        -------
        prisma.models.PurchaseOrder
            The deleted PurchaseOrder record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        purchaseorder = PurchaseOrder.prisma().delete(
            where={
                'id': 'eejajbid',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PurchaseOrderWhereUniqueInput,
        include: Optional[types.PurchaseOrderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PurchaseOrder record.

        Parameters
        ----------
        where
            PurchaseOrder filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model

        Returns
        -------
        prisma.models.PurchaseOrder
            The found PurchaseOrder record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        purchaseorder = PurchaseOrder.prisma().find_unique(
            where={
                'id': 'efhdcdaie',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PurchaseOrderWhereUniqueInput,
        include: Optional[types.PurchaseOrderInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PurchaseOrder record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PurchaseOrder filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model

        Returns
        -------
        prisma.models.PurchaseOrder
            The found PurchaseOrder record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        purchaseorder = PurchaseOrder.prisma().find_unique_or_raise(
            where={
                'id': 'cadejecgbd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PurchaseOrderWhereInput] = None,
        cursor: Optional[types.PurchaseOrderWhereUniqueInput] = None,
        include: Optional[types.PurchaseOrderInclude] = None,
        order: Optional[Union[types.PurchaseOrderOrderByInput, List[types.PurchaseOrderOrderByInput]]] = None,
        distinct: Optional[List[types.PurchaseOrderScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PurchaseOrder records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PurchaseOrder records returned
        skip
            Ignore the first N results
        where
            PurchaseOrder filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model
        order
            Order the returned PurchaseOrder records by any field
        distinct
            Filter PurchaseOrder records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PurchaseOrder]
            The list of all PurchaseOrder records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PurchaseOrder records
        purchaseorders = PurchaseOrder.prisma().find_many(take=10)

        # find the first 5 PurchaseOrder records ordered by the createdById field
        purchaseorders = PurchaseOrder.prisma().find_many(
            take=5,
            order={
                'createdById': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PurchaseOrderWhereInput] = None,
        cursor: Optional[types.PurchaseOrderWhereUniqueInput] = None,
        include: Optional[types.PurchaseOrderInclude] = None,
        order: Optional[Union[types.PurchaseOrderOrderByInput, List[types.PurchaseOrderOrderByInput]]] = None,
        distinct: Optional[List[types.PurchaseOrderScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PurchaseOrder record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PurchaseOrder filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model
        order
            Order the returned PurchaseOrder records by any field
        distinct
            Filter PurchaseOrder records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PurchaseOrder
            The first PurchaseOrder record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PurchaseOrder record ordered by the approvedById field
        purchaseorder = PurchaseOrder.prisma().find_first(
            skip=1,
            order={
                'approvedById': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PurchaseOrderWhereInput] = None,
        cursor: Optional[types.PurchaseOrderWhereUniqueInput] = None,
        include: Optional[types.PurchaseOrderInclude] = None,
        order: Optional[Union[types.PurchaseOrderOrderByInput, List[types.PurchaseOrderOrderByInput]]] = None,
        distinct: Optional[List[types.PurchaseOrderScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PurchaseOrder record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PurchaseOrder filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model
        order
            Order the returned PurchaseOrder records by any field
        distinct
            Filter PurchaseOrder records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PurchaseOrder
            The first PurchaseOrder record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PurchaseOrder record ordered by the paidById field
        purchaseorder = PurchaseOrder.prisma().find_first_or_raise(
            skip=1,
            order={
                'paidById': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PurchaseOrderUpdateInput,
        where: types.PurchaseOrderWhereUniqueInput,
        include: Optional[types.PurchaseOrderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PurchaseOrder record.

        Parameters
        ----------
        data
            PurchaseOrder record data specifying what to update
        where
            PurchaseOrder filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model

        Returns
        -------
        prisma.models.PurchaseOrder
            The updated PurchaseOrder record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        purchaseorder = PurchaseOrder.prisma().update(
            where={
                'id': 'bahjhjjhcc',
            },
            data={
                # data to update the PurchaseOrder record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PurchaseOrderWhereUniqueInput,
        data: types.PurchaseOrderUpsertInput,
        include: Optional[types.PurchaseOrderInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PurchaseOrder filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PurchaseOrder model

        Returns
        -------
        prisma.models.PurchaseOrder
            The created or updated PurchaseOrder record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        purchaseorder = PurchaseOrder.prisma().upsert(
            where={
                'id': 'ebhbhbdff',
            },
            data={
                'create': {
                    'id': 'ebhbhbdff',
                    'eventId': 'bggedbjggi',
                    'createdById': 'hgbafifcf',
                    'provider': 'bejiecfecg',
                    'concept': 'bjgacaeagh',
                    'totalAmount': 1440871456.151564,
                    'currency': 'jbgheibja',
                },
                'update': {
                    'eventId': 'bggedbjggi',
                    'createdById': 'hgbafifcf',
                    'provider': 'bejiecfecg',
                    'concept': 'bjgacaeagh',
                    'totalAmount': 1440871456.151564,
                    'currency': 'jbgheibja',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PurchaseOrderUpdateManyMutationInput,
        where: types.PurchaseOrderWhereInput,
    ) -> int:
        """Update multiple PurchaseOrder records

        Parameters
        ----------
        data
            PurchaseOrder data to update the selected PurchaseOrder records to
        where
            Filter to select the PurchaseOrder records to update

        Returns
        -------
        int
            The total number of PurchaseOrder records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PurchaseOrder records
        total = PurchaseOrder.prisma().update_many(
            data={
                'responsibleId': 'bdiefcdfhg'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PurchaseOrderWhereInput] = None,
        cursor: Optional[types.PurchaseOrderWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PurchaseOrder records present in the database

        Parameters
        ----------
        select
            Select the PurchaseOrder fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PurchaseOrder filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PurchaseOrderCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PurchaseOrder.prisma().count()

        # results: prisma.types.PurchaseOrderCountAggregateOutput
        results = PurchaseOrder.prisma().count(
            select={
                '_all': True,
                'provider': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PurchaseOrderCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PurchaseOrderWhereInput] = None,
        cursor: Optional[types.PurchaseOrderWhereUniqueInput] = None,
    ) -> types.PurchaseOrderCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PurchaseOrderCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PurchaseOrderWhereInput] = None,
        cursor: Optional[types.PurchaseOrderWhereUniqueInput] = None,
    ) -> Union[int, types.PurchaseOrderCountAggregateOutput]:
        """Count the number of PurchaseOrder records present in the database

        Parameters
        ----------
        select
            Select the PurchaseOrder fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PurchaseOrder filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PurchaseOrderCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PurchaseOrder.prisma().count()

        # results: prisma.types.PurchaseOrderCountAggregateOutput
        results = PurchaseOrder.prisma().count(
            select={
                '_all': True,
                'concept': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PurchaseOrderCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PurchaseOrderWhereInput] = None
    ) -> int:
        """Delete multiple PurchaseOrder records.

        Parameters
        ----------
        where
            Optional PurchaseOrder filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PurchaseOrder records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PurchaseOrder records
        total = PurchaseOrder.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PurchaseOrderScalarFieldKeys'],
        *,
        where: Optional['types.PurchaseOrderWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PurchaseOrderAvgAggregateInput'] = None,
        sum: Optional['types.PurchaseOrderSumAggregateInput'] = None,
        min: Optional['types.PurchaseOrderMinAggregateInput'] = None,
        max: Optional['types.PurchaseOrderMaxAggregateInput'] = None,
        having: Optional['types.PurchaseOrderScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PurchaseOrderCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PurchaseOrderScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PurchaseOrderScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PurchaseOrderGroupByOutput']:
        """Group PurchaseOrder records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PurchaseOrder fields to group records by
        where
            PurchaseOrder filter to select records
        take
            Limit the maximum number of PurchaseOrder records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PurchaseOrderGroupByOutput]
            A list of dictionaries representing the PurchaseOrder record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PurchaseOrder records by totalAmount values
        # and count how many records are in each group
        results = PurchaseOrder.prisma().group_by(
            ['totalAmount'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PaypalPaymentDetailActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PaypalPaymentDetail]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PaypalPaymentDetail.prisma().query_raw(
            'SELECT * FROM PaypalPaymentDetail WHERE id = $1',
            'cheifeghd',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PaypalPaymentDetail.prisma().query_first(
            'SELECT * FROM PaypalPaymentDetail WHERE paymentId = $1',
            'fgijheefe',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PaypalPaymentDetailCreateInput,
        include: Optional[types.PaypalPaymentDetailInclude] = None
    ) -> _PrismaModelT:
        """Create a new PaypalPaymentDetail record.

        Parameters
        ----------
        data
            PaypalPaymentDetail record data
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The created PaypalPaymentDetail record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PaypalPaymentDetail record from just the required fields
        paypalpaymentdetail = PaypalPaymentDetail.prisma().create(
            data={
                # data to create a PaypalPaymentDetail record
                'paymentId': 'hcbgbhfch',
                'orderId': 'bfbbbgbfhc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PaypalPaymentDetailCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PaypalPaymentDetail records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PaypalPaymentDetail record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PaypalPaymentDetail.prisma().create_many(
            data=[
                {
                    # data to create a PaypalPaymentDetail record
                    'paymentId': 'ibijjdeb',
                    'orderId': 'caehiccddi',
                },
                {
                    # data to create a PaypalPaymentDetail record
                    'paymentId': 'bgcahjbafj',
                    'orderId': 'bihhgeihca',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PaypalPaymentDetailWhereUniqueInput,
        include: Optional[types.PaypalPaymentDetailInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PaypalPaymentDetail record.

        Parameters
        ----------
        where
            PaypalPaymentDetail filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The deleted PaypalPaymentDetail record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        paypalpaymentdetail = PaypalPaymentDetail.prisma().delete(
            where={
                'id': 'bdgbfahbef',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PaypalPaymentDetailWhereUniqueInput,
        include: Optional[types.PaypalPaymentDetailInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PaypalPaymentDetail record.

        Parameters
        ----------
        where
            PaypalPaymentDetail filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The found PaypalPaymentDetail record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        paypalpaymentdetail = PaypalPaymentDetail.prisma().find_unique(
            where={
                'id': 'fccjhidic',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PaypalPaymentDetailWhereUniqueInput,
        include: Optional[types.PaypalPaymentDetailInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PaypalPaymentDetail record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PaypalPaymentDetail filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The found PaypalPaymentDetail record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        paypalpaymentdetail = PaypalPaymentDetail.prisma().find_unique_or_raise(
            where={
                'id': 'bdgfdgdaff',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None,
        cursor: Optional[types.PaypalPaymentDetailWhereUniqueInput] = None,
        include: Optional[types.PaypalPaymentDetailInclude] = None,
        order: Optional[Union[types.PaypalPaymentDetailOrderByInput, List[types.PaypalPaymentDetailOrderByInput]]] = None,
        distinct: Optional[List[types.PaypalPaymentDetailScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PaypalPaymentDetail records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PaypalPaymentDetail records returned
        skip
            Ignore the first N results
        where
            PaypalPaymentDetail filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model
        order
            Order the returned PaypalPaymentDetail records by any field
        distinct
            Filter PaypalPaymentDetail records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PaypalPaymentDetail]
            The list of all PaypalPaymentDetail records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PaypalPaymentDetail records
        paypalpaymentdetails = PaypalPaymentDetail.prisma().find_many(take=10)

        # find the first 5 PaypalPaymentDetail records ordered by the orderId field
        paypalpaymentdetails = PaypalPaymentDetail.prisma().find_many(
            take=5,
            order={
                'orderId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None,
        cursor: Optional[types.PaypalPaymentDetailWhereUniqueInput] = None,
        include: Optional[types.PaypalPaymentDetailInclude] = None,
        order: Optional[Union[types.PaypalPaymentDetailOrderByInput, List[types.PaypalPaymentDetailOrderByInput]]] = None,
        distinct: Optional[List[types.PaypalPaymentDetailScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PaypalPaymentDetail record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PaypalPaymentDetail filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model
        order
            Order the returned PaypalPaymentDetail records by any field
        distinct
            Filter PaypalPaymentDetail records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The first PaypalPaymentDetail record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PaypalPaymentDetail record ordered by the captureId field
        paypalpaymentdetail = PaypalPaymentDetail.prisma().find_first(
            skip=1,
            order={
                'captureId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None,
        cursor: Optional[types.PaypalPaymentDetailWhereUniqueInput] = None,
        include: Optional[types.PaypalPaymentDetailInclude] = None,
        order: Optional[Union[types.PaypalPaymentDetailOrderByInput, List[types.PaypalPaymentDetailOrderByInput]]] = None,
        distinct: Optional[List[types.PaypalPaymentDetailScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PaypalPaymentDetail record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PaypalPaymentDetail filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model
        order
            Order the returned PaypalPaymentDetail records by any field
        distinct
            Filter PaypalPaymentDetail records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The first PaypalPaymentDetail record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PaypalPaymentDetail record ordered by the payerId field
        paypalpaymentdetail = PaypalPaymentDetail.prisma().find_first_or_raise(
            skip=1,
            order={
                'payerId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PaypalPaymentDetailUpdateInput,
        where: types.PaypalPaymentDetailWhereUniqueInput,
        include: Optional[types.PaypalPaymentDetailInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PaypalPaymentDetail record.

        Parameters
        ----------
        data
            PaypalPaymentDetail record data specifying what to update
        where
            PaypalPaymentDetail filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The updated PaypalPaymentDetail record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        paypalpaymentdetail = PaypalPaymentDetail.prisma().update(
            where={
                'id': 'bicgeaiaga',
            },
            data={
                # data to update the PaypalPaymentDetail record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PaypalPaymentDetailWhereUniqueInput,
        data: types.PaypalPaymentDetailUpsertInput,
        include: Optional[types.PaypalPaymentDetailInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PaypalPaymentDetail filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PaypalPaymentDetail model

        Returns
        -------
        prisma.models.PaypalPaymentDetail
            The created or updated PaypalPaymentDetail record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        paypalpaymentdetail = PaypalPaymentDetail.prisma().upsert(
            where={
                'id': 'fajhhafab',
            },
            data={
                'create': {
                    'id': 'fajhhafab',
                    'paymentId': 'bgcahjbafj',
                    'orderId': 'bihhgeihca',
                },
                'update': {
                    'paymentId': 'bgcahjbafj',
                    'orderId': 'bihhgeihca',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PaypalPaymentDetailUpdateManyMutationInput,
        where: types.PaypalPaymentDetailWhereInput,
    ) -> int:
        """Update multiple PaypalPaymentDetail records

        Parameters
        ----------
        data
            PaypalPaymentDetail data to update the selected PaypalPaymentDetail records to
        where
            Filter to select the PaypalPaymentDetail records to update

        Returns
        -------
        int
            The total number of PaypalPaymentDetail records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PaypalPaymentDetail records
        total = PaypalPaymentDetail.prisma().update_many(
            data={
                'payerEmail': 'bfeiccieec'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None,
        cursor: Optional[types.PaypalPaymentDetailWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PaypalPaymentDetail records present in the database

        Parameters
        ----------
        select
            Select the PaypalPaymentDetail fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PaypalPaymentDetail filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PaypalPaymentDetailCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PaypalPaymentDetail.prisma().count()

        # results: prisma.types.PaypalPaymentDetailCountAggregateOutput
        results = PaypalPaymentDetail.prisma().count(
            select={
                '_all': True,
                'rawData': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PaypalPaymentDetailCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None,
        cursor: Optional[types.PaypalPaymentDetailWhereUniqueInput] = None,
    ) -> types.PaypalPaymentDetailCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PaypalPaymentDetailCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None,
        cursor: Optional[types.PaypalPaymentDetailWhereUniqueInput] = None,
    ) -> Union[int, types.PaypalPaymentDetailCountAggregateOutput]:
        """Count the number of PaypalPaymentDetail records present in the database

        Parameters
        ----------
        select
            Select the PaypalPaymentDetail fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PaypalPaymentDetail filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PaypalPaymentDetailCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PaypalPaymentDetail.prisma().count()

        # results: prisma.types.PaypalPaymentDetailCountAggregateOutput
        results = PaypalPaymentDetail.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PaypalPaymentDetailCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PaypalPaymentDetailWhereInput] = None
    ) -> int:
        """Delete multiple PaypalPaymentDetail records.

        Parameters
        ----------
        where
            Optional PaypalPaymentDetail filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PaypalPaymentDetail records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PaypalPaymentDetail records
        total = PaypalPaymentDetail.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PaypalPaymentDetailScalarFieldKeys'],
        *,
        where: Optional['types.PaypalPaymentDetailWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PaypalPaymentDetailAvgAggregateInput'] = None,
        sum: Optional['types.PaypalPaymentDetailSumAggregateInput'] = None,
        min: Optional['types.PaypalPaymentDetailMinAggregateInput'] = None,
        max: Optional['types.PaypalPaymentDetailMaxAggregateInput'] = None,
        having: Optional['types.PaypalPaymentDetailScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PaypalPaymentDetailCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PaypalPaymentDetailScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PaypalPaymentDetailScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PaypalPaymentDetailGroupByOutput']:
        """Group PaypalPaymentDetail records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PaypalPaymentDetail fields to group records by
        where
            PaypalPaymentDetail filter to select records
        take
            Limit the maximum number of PaypalPaymentDetail records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PaypalPaymentDetailGroupByOutput]
            A list of dictionaries representing the PaypalPaymentDetail record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PaypalPaymentDetail records by updatedAt values
        # and count how many records are in each group
        results = PaypalPaymentDetail.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FinancialMovementToPurchaseOrderActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FinancialMovementToPurchaseOrder]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FinancialMovementToPurchaseOrder.prisma().query_raw(
            'SELECT * FROM FinancialMovementToPurchaseOrder WHERE A = $1',
            'hciegiihf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FinancialMovementToPurchaseOrder.prisma().query_first(
            'SELECT * FROM FinancialMovementToPurchaseOrder WHERE B = $1',
            'bahifjfga',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FinancialMovementToPurchaseOrderCreateInput,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None
    ) -> _PrismaModelT:
        """Create a new FinancialMovementToPurchaseOrder record.

        Parameters
        ----------
        data
            FinancialMovementToPurchaseOrder record data
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The created FinancialMovementToPurchaseOrder record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FinancialMovementToPurchaseOrder record from just the required fields
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().create(
            data={
                # data to create a FinancialMovementToPurchaseOrder record
                'A': 'baebfehjaf',
                'B': 'bjchdacjfa',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FinancialMovementToPurchaseOrderCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FinancialMovementToPurchaseOrder records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FinancialMovementToPurchaseOrder record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FinancialMovementToPurchaseOrder.prisma().create_many(
            data=[
                {
                    # data to create a FinancialMovementToPurchaseOrder record
                    'A': 'fcadcaihb',
                    'B': 'bbdcibggga',
                },
                {
                    # data to create a FinancialMovementToPurchaseOrder record
                    'A': 'bagbibfheb',
                    'B': 'bidgcabjag',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FinancialMovementToPurchaseOrderWhereUniqueInput,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FinancialMovementToPurchaseOrder record.

        Parameters
        ----------
        where
            FinancialMovementToPurchaseOrder filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The deleted FinancialMovementToPurchaseOrder record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().delete(
            where={
                # FinancialMovementToPurchaseOrder where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FinancialMovementToPurchaseOrderWhereUniqueInput,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FinancialMovementToPurchaseOrder record.

        Parameters
        ----------
        where
            FinancialMovementToPurchaseOrder filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The found FinancialMovementToPurchaseOrder record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().find_unique(
            where={
                # FinancialMovementToPurchaseOrder where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FinancialMovementToPurchaseOrderWhereUniqueInput,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FinancialMovementToPurchaseOrder record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FinancialMovementToPurchaseOrder filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The found FinancialMovementToPurchaseOrder record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().find_unique_or_raise(
            where={
                # FinancialMovementToPurchaseOrder where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None,
        cursor: Optional[types.FinancialMovementToPurchaseOrderWhereUniqueInput] = None,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None,
        order: Optional[Union[types.FinancialMovementToPurchaseOrderOrderByInput, List[types.FinancialMovementToPurchaseOrderOrderByInput]]] = None,
        distinct: Optional[List[types.FinancialMovementToPurchaseOrderScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FinancialMovementToPurchaseOrder records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FinancialMovementToPurchaseOrder records returned
        skip
            Ignore the first N results
        where
            FinancialMovementToPurchaseOrder filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model
        order
            Order the returned FinancialMovementToPurchaseOrder records by any field
        distinct
            Filter FinancialMovementToPurchaseOrder records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FinancialMovementToPurchaseOrder]
            The list of all FinancialMovementToPurchaseOrder records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FinancialMovementToPurchaseOrder records
        financialmovementtopurchaseorders = FinancialMovementToPurchaseOrder.prisma().find_many(take=10)

        # find the first 5 FinancialMovementToPurchaseOrder records ordered by the A field
        financialmovementtopurchaseorders = FinancialMovementToPurchaseOrder.prisma().find_many(
            take=5,
            order={
                'A': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None,
        cursor: Optional[types.FinancialMovementToPurchaseOrderWhereUniqueInput] = None,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None,
        order: Optional[Union[types.FinancialMovementToPurchaseOrderOrderByInput, List[types.FinancialMovementToPurchaseOrderOrderByInput]]] = None,
        distinct: Optional[List[types.FinancialMovementToPurchaseOrderScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FinancialMovementToPurchaseOrder record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FinancialMovementToPurchaseOrder filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model
        order
            Order the returned FinancialMovementToPurchaseOrder records by any field
        distinct
            Filter FinancialMovementToPurchaseOrder records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The first FinancialMovementToPurchaseOrder record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FinancialMovementToPurchaseOrder record ordered by the B field
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().find_first(
            skip=1,
            order={
                'B': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None,
        cursor: Optional[types.FinancialMovementToPurchaseOrderWhereUniqueInput] = None,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None,
        order: Optional[Union[types.FinancialMovementToPurchaseOrderOrderByInput, List[types.FinancialMovementToPurchaseOrderOrderByInput]]] = None,
        distinct: Optional[List[types.FinancialMovementToPurchaseOrderScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FinancialMovementToPurchaseOrder record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FinancialMovementToPurchaseOrder filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model
        order
            Order the returned FinancialMovementToPurchaseOrder records by any field
        distinct
            Filter FinancialMovementToPurchaseOrder records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The first FinancialMovementToPurchaseOrder record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FinancialMovementToPurchaseOrder record ordered by the A field
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().find_first_or_raise(
            skip=1,
            order={
                'A': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FinancialMovementToPurchaseOrderUpdateInput,
        where: types.FinancialMovementToPurchaseOrderWhereUniqueInput,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FinancialMovementToPurchaseOrder record.

        Parameters
        ----------
        data
            FinancialMovementToPurchaseOrder record data specifying what to update
        where
            FinancialMovementToPurchaseOrder filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The updated FinancialMovementToPurchaseOrder record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().update(
            where={
                # FinancialMovementToPurchaseOrder where unique filter

            },
            data={
                # data to update the FinancialMovementToPurchaseOrder record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FinancialMovementToPurchaseOrderWhereUniqueInput,
        data: types.FinancialMovementToPurchaseOrderUpsertInput,
        include: Optional[types.FinancialMovementToPurchaseOrderInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FinancialMovementToPurchaseOrder filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FinancialMovementToPurchaseOrder model

        Returns
        -------
        prisma.models.FinancialMovementToPurchaseOrder
            The created or updated FinancialMovementToPurchaseOrder record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        financialmovementtopurchaseorder = FinancialMovementToPurchaseOrder.prisma().upsert(
            where={
                # FinancialMovementToPurchaseOrder where unique filter
            },
            data={
                'create': {
                    # FinancialMovementToPurchaseOrder data to be set if the record does not exist
                },
                'update': {
                    # FinancialMovementToPurchaseOrder data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FinancialMovementToPurchaseOrderUpdateManyMutationInput,
        where: types.FinancialMovementToPurchaseOrderWhereInput,
    ) -> int:
        """Update multiple FinancialMovementToPurchaseOrder records

        Parameters
        ----------
        data
            FinancialMovementToPurchaseOrder data to update the selected FinancialMovementToPurchaseOrder records to
        where
            Filter to select the FinancialMovementToPurchaseOrder records to update

        Returns
        -------
        int
            The total number of FinancialMovementToPurchaseOrder records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FinancialMovementToPurchaseOrder records
        total = FinancialMovementToPurchaseOrder.prisma().update_many(
            data={
                'B': 'hechjdjih'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None,
        cursor: Optional[types.FinancialMovementToPurchaseOrderWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FinancialMovementToPurchaseOrder records present in the database

        Parameters
        ----------
        select
            Select the FinancialMovementToPurchaseOrder fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FinancialMovementToPurchaseOrder filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FinancialMovementToPurchaseOrderCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FinancialMovementToPurchaseOrder.prisma().count()

        # results: prisma.types.FinancialMovementToPurchaseOrderCountAggregateOutput
        results = FinancialMovementToPurchaseOrder.prisma().count(
            select={
                '_all': True,
                'A': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FinancialMovementToPurchaseOrderCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None,
        cursor: Optional[types.FinancialMovementToPurchaseOrderWhereUniqueInput] = None,
    ) -> types.FinancialMovementToPurchaseOrderCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FinancialMovementToPurchaseOrderCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None,
        cursor: Optional[types.FinancialMovementToPurchaseOrderWhereUniqueInput] = None,
    ) -> Union[int, types.FinancialMovementToPurchaseOrderCountAggregateOutput]:
        """Count the number of FinancialMovementToPurchaseOrder records present in the database

        Parameters
        ----------
        select
            Select the FinancialMovementToPurchaseOrder fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FinancialMovementToPurchaseOrder filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FinancialMovementToPurchaseOrderCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FinancialMovementToPurchaseOrder.prisma().count()

        # results: prisma.types.FinancialMovementToPurchaseOrderCountAggregateOutput
        results = FinancialMovementToPurchaseOrder.prisma().count(
            select={
                '_all': True,
                'B': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FinancialMovementToPurchaseOrderCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FinancialMovementToPurchaseOrderWhereInput] = None
    ) -> int:
        """Delete multiple FinancialMovementToPurchaseOrder records.

        Parameters
        ----------
        where
            Optional FinancialMovementToPurchaseOrder filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FinancialMovementToPurchaseOrder records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FinancialMovementToPurchaseOrder records
        total = FinancialMovementToPurchaseOrder.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FinancialMovementToPurchaseOrderScalarFieldKeys'],
        *,
        where: Optional['types.FinancialMovementToPurchaseOrderWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FinancialMovementToPurchaseOrderAvgAggregateInput'] = None,
        sum: Optional['types.FinancialMovementToPurchaseOrderSumAggregateInput'] = None,
        min: Optional['types.FinancialMovementToPurchaseOrderMinAggregateInput'] = None,
        max: Optional['types.FinancialMovementToPurchaseOrderMaxAggregateInput'] = None,
        having: Optional['types.FinancialMovementToPurchaseOrderScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FinancialMovementToPurchaseOrderCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FinancialMovementToPurchaseOrderScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FinancialMovementToPurchaseOrderScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FinancialMovementToPurchaseOrderGroupByOutput']:
        """Group FinancialMovementToPurchaseOrder records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FinancialMovementToPurchaseOrder fields to group records by
        where
            FinancialMovementToPurchaseOrder filter to select records
        take
            Limit the maximum number of FinancialMovementToPurchaseOrder records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FinancialMovementToPurchaseOrderGroupByOutput]
            A list of dictionaries representing the FinancialMovementToPurchaseOrder record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FinancialMovementToPurchaseOrder records by A values
        # and count how many records are in each group
        results = FinancialMovementToPurchaseOrder.prisma().group_by(
            ['A'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class OrganizationToUserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.OrganizationToUser]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = OrganizationToUser.prisma().query_raw(
            'SELECT * FROM OrganizationToUser WHERE A = $1',
            'cgfjefhci',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.OrganizationToUser
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = OrganizationToUser.prisma().query_first(
            'SELECT * FROM OrganizationToUser WHERE B = $1',
            'egcidbiab',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.OrganizationToUserCreateInput,
        include: Optional[types.OrganizationToUserInclude] = None
    ) -> _PrismaModelT:
        """Create a new OrganizationToUser record.

        Parameters
        ----------
        data
            OrganizationToUser record data
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model

        Returns
        -------
        prisma.models.OrganizationToUser
            The created OrganizationToUser record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a OrganizationToUser record from just the required fields
        organizationtouser = OrganizationToUser.prisma().create(
            data={
                # data to create a OrganizationToUser record
                'A': 'cehjjfgeg',
                'B': 'hiaaaeeeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.OrganizationToUserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple OrganizationToUser records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of OrganizationToUser record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = OrganizationToUser.prisma().create_many(
            data=[
                {
                    # data to create a OrganizationToUser record
                    'A': 'ediajdeie',
                    'B': 'jdgefgafd',
                },
                {
                    # data to create a OrganizationToUser record
                    'A': 'bhhihbfeac',
                    'B': 'bajegedfhj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.OrganizationToUserWhereUniqueInput,
        include: Optional[types.OrganizationToUserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single OrganizationToUser record.

        Parameters
        ----------
        where
            OrganizationToUser filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model

        Returns
        -------
        prisma.models.OrganizationToUser
            The deleted OrganizationToUser record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organizationtouser = OrganizationToUser.prisma().delete(
            where={
                # OrganizationToUser where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.OrganizationToUserWhereUniqueInput,
        include: Optional[types.OrganizationToUserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique OrganizationToUser record.

        Parameters
        ----------
        where
            OrganizationToUser filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model

        Returns
        -------
        prisma.models.OrganizationToUser
            The found OrganizationToUser record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organizationtouser = OrganizationToUser.prisma().find_unique(
            where={
                # OrganizationToUser where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.OrganizationToUserWhereUniqueInput,
        include: Optional[types.OrganizationToUserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique OrganizationToUser record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            OrganizationToUser filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model

        Returns
        -------
        prisma.models.OrganizationToUser
            The found OrganizationToUser record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organizationtouser = OrganizationToUser.prisma().find_unique_or_raise(
            where={
                # OrganizationToUser where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationToUserWhereInput] = None,
        cursor: Optional[types.OrganizationToUserWhereUniqueInput] = None,
        include: Optional[types.OrganizationToUserInclude] = None,
        order: Optional[Union[types.OrganizationToUserOrderByInput, List[types.OrganizationToUserOrderByInput]]] = None,
        distinct: Optional[List[types.OrganizationToUserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple OrganizationToUser records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of OrganizationToUser records returned
        skip
            Ignore the first N results
        where
            OrganizationToUser filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model
        order
            Order the returned OrganizationToUser records by any field
        distinct
            Filter OrganizationToUser records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.OrganizationToUser]
            The list of all OrganizationToUser records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 OrganizationToUser records
        organizationtousers = OrganizationToUser.prisma().find_many(take=10)

        # find the first 5 OrganizationToUser records ordered by the A field
        organizationtousers = OrganizationToUser.prisma().find_many(
            take=5,
            order={
                'A': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationToUserWhereInput] = None,
        cursor: Optional[types.OrganizationToUserWhereUniqueInput] = None,
        include: Optional[types.OrganizationToUserInclude] = None,
        order: Optional[Union[types.OrganizationToUserOrderByInput, List[types.OrganizationToUserOrderByInput]]] = None,
        distinct: Optional[List[types.OrganizationToUserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single OrganizationToUser record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            OrganizationToUser filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model
        order
            Order the returned OrganizationToUser records by any field
        distinct
            Filter OrganizationToUser records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.OrganizationToUser
            The first OrganizationToUser record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second OrganizationToUser record ordered by the B field
        organizationtouser = OrganizationToUser.prisma().find_first(
            skip=1,
            order={
                'B': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationToUserWhereInput] = None,
        cursor: Optional[types.OrganizationToUserWhereUniqueInput] = None,
        include: Optional[types.OrganizationToUserInclude] = None,
        order: Optional[Union[types.OrganizationToUserOrderByInput, List[types.OrganizationToUserOrderByInput]]] = None,
        distinct: Optional[List[types.OrganizationToUserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single OrganizationToUser record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            OrganizationToUser filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model
        order
            Order the returned OrganizationToUser records by any field
        distinct
            Filter OrganizationToUser records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.OrganizationToUser
            The first OrganizationToUser record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second OrganizationToUser record ordered by the A field
        organizationtouser = OrganizationToUser.prisma().find_first_or_raise(
            skip=1,
            order={
                'A': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.OrganizationToUserUpdateInput,
        where: types.OrganizationToUserWhereUniqueInput,
        include: Optional[types.OrganizationToUserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single OrganizationToUser record.

        Parameters
        ----------
        data
            OrganizationToUser record data specifying what to update
        where
            OrganizationToUser filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model

        Returns
        -------
        prisma.models.OrganizationToUser
            The updated OrganizationToUser record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        organizationtouser = OrganizationToUser.prisma().update(
            where={
                # OrganizationToUser where unique filter

            },
            data={
                # data to update the OrganizationToUser record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.OrganizationToUserWhereUniqueInput,
        data: types.OrganizationToUserUpsertInput,
        include: Optional[types.OrganizationToUserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            OrganizationToUser filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned OrganizationToUser model

        Returns
        -------
        prisma.models.OrganizationToUser
            The created or updated OrganizationToUser record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        organizationtouser = OrganizationToUser.prisma().upsert(
            where={
                # OrganizationToUser where unique filter
            },
            data={
                'create': {
                    # OrganizationToUser data to be set if the record does not exist
                },
                'update': {
                    # OrganizationToUser data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.OrganizationToUserUpdateManyMutationInput,
        where: types.OrganizationToUserWhereInput,
    ) -> int:
        """Update multiple OrganizationToUser records

        Parameters
        ----------
        data
            OrganizationToUser data to update the selected OrganizationToUser records to
        where
            Filter to select the OrganizationToUser records to update

        Returns
        -------
        int
            The total number of OrganizationToUser records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all OrganizationToUser records
        total = OrganizationToUser.prisma().update_many(
            data={
                'B': 'bhfjihjfhg'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationToUserWhereInput] = None,
        cursor: Optional[types.OrganizationToUserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of OrganizationToUser records present in the database

        Parameters
        ----------
        select
            Select the OrganizationToUser fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            OrganizationToUser filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.OrganizationToUserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = OrganizationToUser.prisma().count()

        # results: prisma.types.OrganizationToUserCountAggregateOutput
        results = OrganizationToUser.prisma().count(
            select={
                '_all': True,
                'A': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.OrganizationToUserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationToUserWhereInput] = None,
        cursor: Optional[types.OrganizationToUserWhereUniqueInput] = None,
    ) -> types.OrganizationToUserCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.OrganizationToUserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.OrganizationToUserWhereInput] = None,
        cursor: Optional[types.OrganizationToUserWhereUniqueInput] = None,
    ) -> Union[int, types.OrganizationToUserCountAggregateOutput]:
        """Count the number of OrganizationToUser records present in the database

        Parameters
        ----------
        select
            Select the OrganizationToUser fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            OrganizationToUser filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.OrganizationToUserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = OrganizationToUser.prisma().count()

        # results: prisma.types.OrganizationToUserCountAggregateOutput
        results = OrganizationToUser.prisma().count(
            select={
                '_all': True,
                'B': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.OrganizationToUserCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.OrganizationToUserWhereInput] = None
    ) -> int:
        """Delete multiple OrganizationToUser records.

        Parameters
        ----------
        where
            Optional OrganizationToUser filter to find the records to be deleted

        Returns
        -------
        int
            The total number of OrganizationToUser records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all OrganizationToUser records
        total = OrganizationToUser.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.OrganizationToUserScalarFieldKeys'],
        *,
        where: Optional['types.OrganizationToUserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.OrganizationToUserAvgAggregateInput'] = None,
        sum: Optional['types.OrganizationToUserSumAggregateInput'] = None,
        min: Optional['types.OrganizationToUserMinAggregateInput'] = None,
        max: Optional['types.OrganizationToUserMaxAggregateInput'] = None,
        having: Optional['types.OrganizationToUserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.OrganizationToUserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.OrganizationToUserScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.OrganizationToUserScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.OrganizationToUserGroupByOutput']:
        """Group OrganizationToUser records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar OrganizationToUser fields to group records by
        where
            OrganizationToUser filter to select records
        take
            Limit the maximum number of OrganizationToUser records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.OrganizationToUserGroupByOutput]
            A list of dictionaries representing the OrganizationToUser record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group OrganizationToUser records by A values
        # and count how many records are in each group
        results = OrganizationToUser.prisma().group_by(
            ['A'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models